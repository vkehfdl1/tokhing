"use client";import{useState}from"react";import{Button}from"@/components/ui/button";import{Card}from"@/components/ui/card";import{Input}from"@/components/ui/input";export default function SettlementRecovery(){const[id,setId]=useState("4"),[data,setData]=useState<any>();async function preview(){const r=await fetch(`/api/admin/settlement-recovery?id=${id}`);setData((await r.json()).preview)}return <section className="mt-8"><h2 className="text-2xl font-bold">정산 복구</h2><Card className="rounded-2xl p-4"><Input aria-label="정산 마켓 ID" value={id} onChange={e=>setId(e.target.value)}/><Button onClick={()=>void preview()}>복구 영향 미리보기</Button>{data?<pre className="whitespace-pre-wrap text-xs">{JSON.stringify(data,null,2)}</pre>:null}</Card></section>}
