"use client";import{useEffect,useState}from"react";import{Card}from"@/components/ui/card";export default function SeasonIncidentRecovery(){const[data,setData]=useState<any>();useEffect(()=>{fetch("/api/admin/season-incident?id=1").then(r=>r.json()).then(d=>setData(d.preview))},[]);return <section className="space-y-3"><h2 className="text-2xl font-bold">시즌 사고 복구</h2><Card className="rounded-2xl p-4"><p>일반 상태 편집과 분리된 OWNER 전용 워크플로우</p>{data?<><p>지갑 {data.counts.wallets} · 주문 {data.counts.orders} · 포지션 {data.counts.positions} · 마켓 {data.counts.markets} · 거래 {data.counts.transactions}</p><p>{data.automatic_possible?"자동 복구 가능":`수동 대응 필요: ${data.reason}`}</p></>:null}</Card></section>}
