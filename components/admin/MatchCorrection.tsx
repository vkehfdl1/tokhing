"use client";import{useEffect,useState}from"react";import{Button}from"@/components/ui/button";import{Card}from"@/components/ui/card";import{Input}from"@/components/ui/input";export default function MatchCorrection(){const[id,setId]=useState("901"),[impact,setImpact]=useState<any>(null);async function preview(){const r=await fetch(`/api/admin/match-correction?id=${id}`);setImpact((await r.json()).impact)}useEffect(()=>{void preview()},[]);return <section className="mt-8 space-y-3"><h2 className="text-2xl font-bold">경기 수정</h2><Card className="rounded-2xl p-4"><Input aria-label="경기 ID" value={id} onChange={e=>setId(e.target.value)}/><Button onClick={()=>void preview()}>변경 영향 미리보기</Button>{impact?<p>연결 마켓 {impact.market?.id??0} · 주문 {impact.order_count} · 포지션 {impact.position_count} · {impact.deletable?"삭제 가능":"삭제 차단"}</p>:null}</Card></section>}
