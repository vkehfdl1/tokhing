OpenAI Codex v0.104.0 (research preview)
--------
workdir: /Users/jeffrey/WebstormProjects/lookin/lookin
model: gpt-5.3-codex
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: xhigh
reasoning summaries: auto
session id: 019c9026-0b2a-71c1-b9b6-87a050957453
--------
user
Read AGENTS.md for full project context, file paths, and rules.

Check prd.json and implement story US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹

Acceptance criteria:
- REQ-053 get_leaderboard_balance() RETURNS TABLE(...) RPC 함수. 코인 잔고 기준 내림차순 정렬. 반환: rank, user_id, username, balance.
- REQ-054 get_leaderboard_roi() RETURNS TABLE(...) RPC 함수. 수익률 = (현재잔고 - 누적지급코인) / 누적지급코인 × 100. 반환: rank, user_id, username, roi_percent, current_balance, total_granted.
- REQ-055 리더보드 UI에 '잔고 순위' / '수익률 순위' 탭 전환.
- REQ-056 기존 리더보드 스타일 유지: 1등 tokhin-green 배경 + 흰 텍스트, 2~3등 tokhin-green 텍스트, 나머지 회색.

mcp startup: no servers

thinking
**Confirming mandatory skill usage**
codex
요청하신 `US-008` 구현을 위해 먼저 `AGENTS.md`/`prd.json`의 스토리 상태와 요구사항을 확인하고, 적용해야 하는 스킬 워크플로우부터 로드하겠습니다. 이어서 관련 파일(`lib/api.ts`, `app/leaderboard/page.tsx`, DB SQL 위치)을 찾아 바로 구현하겠습니다.

thinking
**Planning parallel file reads**
exec
/bin/zsh -lc 'pwd && ls -la' in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 51ms:
/Users/jeffrey/WebstormProjects/lookin/lookin
total 1056
drwxr-xr-x@  35 jeffrey  staff    1120 Feb 24 23:16 .
drwxr-xr-x@   3 jeffrey  staff      96 Jul 13  2025 ..
-rw-r--r--@   1 jeffrey  staff     863 Jul 19  2025 .env.local
-rw-r--r--@   1 jeffrey  staff     625 Aug  8  2025 .env.local.example
drwxr-xr-x@  14 jeffrey  staff     448 Feb 24 23:55 .git
-rw-r--r--@   1 jeffrey  staff     491 Jul 13  2025 .gitignore
drwxr-xr-x@  21 jeffrey  staff     672 Feb 24 23:54 .next
drwxr-xr-x@   4 jeffrey  staff     128 Feb 24 22:16 .ralph
-rw-r--r--@   1 jeffrey  staff    3134 Jul 19  2025 ADMIN_SETUP.md
-rw-r--r--@   1 jeffrey  staff    4760 Feb 24 22:11 AGENTS.md
-rw-r--r--@   1 jeffrey  staff     219 Jul 13  2025 GEMINI.md
-rw-r--r--@   1 jeffrey  staff     994 Feb 24 23:55 PROGRESS.md
-rw-r--r--@   1 jeffrey  staff    5029 Jul 19  2025 README.md
-rw-r--r--@   1 jeffrey  staff    4551 Jul 26  2025 RESPONSIVE_DESIGN_SUMMARY.md
drwxr-xr-x@  15 jeffrey  staff     480 Feb 24 23:25 app
drwxr-xr-x@  21 jeffrey  staff     672 Feb 24 23:06 components
-rw-r--r--@   1 jeffrey  staff     427 Jul 13  2025 components.json
-rw-r--r--@   1 jeffrey  staff     393 Jul 13  2025 eslint.config.mjs

thinking
**Reviewing agent capability docs**
exec
/bin/zsh -lc "sed -n '1,260p' AGENTS.md" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
# AGENTS.md — ToKHin' LMSR Prediction Market

## Project Overview

KBO 야구 승부예측 앱(ToKHin')을 LMSR 기반 예측시장으로 전환하는 프로젝트.
기존 "팀 하나 찍기 + 점수제"를 "HOME/AWAY/DRAW 토큰 매수/매도 + 만기 0/100 정산" 시스템으로 완전 대체한다.

## Tech Stack

- **Framework**: Next.js 15 (App Router) + React 19 + TypeScript
- **Styling**: Tailwind CSS 3.4 + shadcn/ui (Radix) + Pretendard 폰트
- **Database**: Supabase (PostgreSQL) — 테이블, RPC 함수, RLS, pg_cron
- **Auth**: 자체 구현 (학번 + SHA-256 해시 비밀번호, Supabase Auth 미사용)
- **Deploy**: Vercel
- **Package Manager**: npm

## Key Files

| Path | Description |
|------|-------------|

thinking
**Preparing to load required skills**
codex
필수 스킬 규칙을 맞추기 위해 먼저 `using-superpowers`, `brainstorming`, `test-driven-development`, `verification-before-completion` 가이드를 빠르게 확인해 현재 작업 흐름에 반영하겠습니다.
exec
/bin/zsh -lc "sed -n '1,220p' /Users/jeffrey/.codex/superpowers/skills/using-superpowers/SKILL.md" in /Users/jeffrey/WebstormProjects/lookin/lookinexec
/bin/zsh -lc "sed -n '1,260p' /Users/jeffrey/.codex/superpowers/skills/brainstorming/SKILL.md" in /Users/jeffrey/WebstormProjects/lookin/lookinexec
/bin/zsh -lc "sed -n '1,260p' /Users/jeffrey/.codex/superpowers/skills/test-driven-development/SKILL.md" in /Users/jeffrey/WebstormProjects/lookin/lookinexec
/bin/zsh -lc "sed -n '1,260p' /Users/jeffrey/.codex/superpowers/skills/verification-before-completion/SKILL.md" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 51ms:
---
name: using-superpowers
description: Use when starting any conversation - establishes how to find and use skills, requiring Skill tool invocation before ANY response including clarifying questions
---

<EXTREMELY-IMPORTANT>
If you think there is even a 1% chance a skill might apply to what you are doing, you ABSOLUTELY MUST invoke the skill.

IF A SKILL APPLIES TO YOUR TASK, YOU DO NOT HAVE A CHOICE. YOU MUST USE IT.

This is not negotiable. This is not optional. You cannot rationalize your way out of this.
</EXTREMELY-IMPORTANT>

## How to Access Skills

**In Claude Code:** Use the `Skill` tool. When you invoke a skill, its content is loaded and presented to you—follow it directly. Never use the Read tool on skill files.

**In other environments:** Check your platform's documentation for how skills are loaded.

# Using Skills
 succeeded in 50ms:
---
name: brainstorming
description: "You MUST use this before any creative work - creating features, building components, adding functionality, or modifying behavior. Explores user intent, requirements and design before implementation."
---

# Brainstorming Ideas Into Designs

## Overview

Help turn ideas into fully formed designs and specs through natural collaborative dialogue.

Start by understanding the current project context, then ask questions one at a time to refine the idea. Once you understand what you're building, present the design and get user approval.

<HARD-GATE>
Do NOT invoke any implementation skill, write any code, scaffold any project, or take any implementation action until you have presented a design and the user has approved it. This applies to EVERY project regardless of perceived simplicity.
</HARD-GATE>

## Anti-Pattern: "This Is Too Simple To Need A Design"

Every project goes through this process. A todo list, a single-function utility, a config change — all of them. "Simple" projects are where unexamined assumptions cause the most wasted work. The design can be short (a few sentences for truly simple projects), but you MUST present it and get approval.
 succeeded in 51ms:
---
name: test-driven-development
description: Use when implementing any feature or bugfix, before writing implementation code
---

# Test-Driven Development (TDD)

## Overview

Write the test first. Watch it fail. Write minimal code to pass.

**Core principle:** If you didn't watch the test fail, you don't know if it tests the right thing.

**Violating the letter of the rules is violating the spirit of the rules.**

## When to Use

**Always:**
- New features
- Bug fixes
 succeeded in 50ms:
---
name: verification-before-completion
description: Use when about to claim work is complete, fixed, or passing, before committing or creating PRs - requires running verification commands and confirming output before making any success claims; evidence before assertions always
---

# Verification Before Completion

## Overview

Claiming work is complete without verification is dishonesty, not efficiency.

**Core principle:** Evidence before claims, always.

**Violating the letter of this rule is violating the spirit of this rule.**

## The Iron Law

```
NO COMPLETION CLAIMS WITHOUT FRESH VERIFICATION EVIDENCE
```

thinking
**Balancing skill use with direct implementation**
codex
관련 구현 위치를 확정하기 위해 `prd.json`의 `US-008` 항목과 현재 `leaderboard` 페이지/`lib/api.ts`/Supabase SQL 파일을 동시에 확인하겠습니다. 결과를 바탕으로 DB RPC와 UI를 한 번에 맞춰 수정하겠습니다.
exec
/bin/zsh -lc "sed -n '1,360p' lib/api.ts" in /Users/jeffrey/WebstormProjects/lookin/lookinexec
/bin/zsh -lc "sed -n '1,260p' app/leaderboard/page.tsx" in /Users/jeffrey/WebstormProjects/lookin/lookinexec
/bin/zsh -lc 'rg -n "US-008|leaderboard|REQ-053|REQ-054|REQ-055|REQ-056" prd.json' in /Users/jeffrey/WebstormProjects/lookin/lookinexec
/bin/zsh -lc 'rg -n "CREATE OR REPLACE FUNCTION|get_leaderboard|leaderboard|wallet|transactions|total_granted" supabase -S' in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 51ms:
"use client";

import { useState, useEffect } from "react";
import { getLeaderboard } from "@/lib/api";
import { useIsMobile } from "@/lib/hooks/useResponsive";
import { useUserSession } from "@/lib/hooks/useUserSession";

type LeaderboardEntry = {
  userId: string;
  name: string;
  student_number: string;
  score: number;
};

export default function LeaderboardPage() {
  const isMobile = useIsMobile();
  const { session, isLoading: isSessionLoading } = useUserSession({
    requireAuth: true,
  });
  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
 succeeded in 51ms:
import { createClient } from "@/lib/supabase/client";

const supabase = createClient();

interface GameData {
  game_date: string;
  game_time?: string;
  home_team_id?: number;
  away_team_id?: number;
  home_pitcher?: string;
  away_pitcher?: string;
  home_score?: number | null;
  away_score?: number | null;
  game_status?: "SCHEDULED" | "LIVE" | "FINISHED";
}

interface LoginRpcResponse {
  success: boolean;
  user_id?: string;
  username?: string;
 succeeded in 51ms:
124:      "id": "US-008",
130:        "REQ-053 get_leaderboard_balance() RETURNS TABLE(...) RPC \ud568\uc218. \ucf54\uc778 \uc794\uace0 \uae30\uc900 \ub0b4\ub9bc\ucc28\uc21c \uc815\ub82c. \ubc18\ud658: rank, user_id, username, balance.",
131:        "REQ-054 get_leaderboard_roi() RETURNS TABLE(...) RPC \ud568\uc218. \uc218\uc775\ub960 = (\ud604\uc7ac\uc794\uace0 - \ub204\uc801\uc9c0\uae09\ucf54\uc778) / \ub204\uc801\uc9c0\uae09\ucf54\uc778 \u00d7 100. \ubc18\ud658: rank, user_id, username, roi_percent, current_balance, total_granted.",
132:        "REQ-055 \ub9ac\ub354\ubcf4\ub4dc UI\uc5d0 '\uc794\uace0 \uc21c\uc704' / '\uc218\uc775\ub960 \uc21c\uc704' \ud0ed \uc804\ud658.",
133:        "REQ-056 \uae30\uc874 \ub9ac\ub354\ubcf4\ub4dc \uc2a4\ud0c0\uc77c \uc720\uc9c0: 1\ub4f1 tokhin-green \ubc30\uacbd + \ud770 \ud14d\uc2a4\ud2b8, 2~3\ub4f1 tokhin-green \ud14d\uc2a4\ud2b8, \ub098\uba38\uc9c0 \ud68c\uc0c9."
 succeeded in 51ms:
supabase/migrations/20260224_us004_wallet_system.sql:4:-- REQ-027: 로그인 성공 시 wallet 자동 생성
supabase/migrations/20260224_us004_wallet_system.sql:5:CREATE OR REPLACE FUNCTION public.login(
supabase/migrations/20260224_us004_wallet_system.sql:40:  INSERT INTO public.wallets (user_id, balance)
supabase/migrations/20260224_us004_wallet_system.sql:54:CREATE OR REPLACE FUNCTION public.distribute_weekly_coins(
supabase/migrations/20260224_us004_wallet_system.sql:70:  INSERT INTO public.wallets (user_id, balance)
supabase/migrations/20260224_us004_wallet_system.sql:75:  WITH updated_wallets AS (
supabase/migrations/20260224_us004_wallet_system.sql:76:    UPDATE public.wallets w
supabase/migrations/20260224_us004_wallet_system.sql:83:  ), inserted_transactions AS (
supabase/migrations/20260224_us004_wallet_system.sql:84:    INSERT INTO public.transactions (
supabase/migrations/20260224_us004_wallet_system.sql:99:    FROM updated_wallets uw
supabase/migrations/20260224_us004_wallet_system.sql:106:  FROM inserted_transactions;
supabase/migrations/20260224_us004_wallet_system.sql:123:CREATE OR REPLACE FUNCTION public.admin_grant_coins(
supabase/migrations/20260224_us004_wallet_system.sql:133:  v_wallet public.wallets%ROWTYPE;
supabase/migrations/20260224_us004_wallet_system.sql:149:  INTO v_wallet
supabase/migrations/20260224_us004_wallet_system.sql:150:  FROM public.wallets
supabase/migrations/20260224_us004_wallet_system.sql:155:    INSERT INTO public.wallets (user_id, balance)
supabase/migrations/20260224_us004_wallet_system.sql:157:    RETURNING * INTO v_wallet;
supabase/migrations/20260224_us004_wallet_system.sql:160:  v_new_balance := v_wallet.balance + p_amount;
supabase/migrations/20260224_us004_wallet_system.sql:162:  UPDATE public.wallets
supabase/migrations/20260224_us004_wallet_system.sql:166:  WHERE id = v_wallet.id;

thinking
**Preparing deeper API scan**
exec
/bin/zsh -lc 'rg -n "leaderboard|getLeaderboard|score|ranking|wallet|roi|balance" lib/api.ts -n' in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
12:  home_score?: number | null;
13:  away_score?: number | null;
27:  balance?: number | string;
42:  new_balance?: number | string;
124:  new_balance?: number | string;
516:      id, game_date, game_status, home_pitcher, away_pitcher, home_score, away_score,
607:            id, game_date, home_score, away_score,
648:      game.home_score > game.away_score ? homeTeam.id : awayTeam.id;
668:  // 1. Fetch all games for the given date that have finished (have scores)
673:            id, game_date, home_score, away_score, game_status,
737:    if (game.home_score !== null && game.away_score !== null) {
738:      if (game.home_score > game.away_score) {
744:      } else if (game.away_score > game.home_score) {
784:export const getLeaderboard = async (startDate?: string, endDate?: string) => {
785:  const { data, error } = await supabase.rpc("get_leaderboard", {
791:    console.error("Error fetching leaderboard:", error);
800:      total_score: number;
805:      score: entry.total_score,
935:      id, game_date, game_status, home_pitcher, away_pitcher, home_score, away_score,
1114:  const newBalance = Number(rpcResult.new_balance);

thinking
**Reviewing leaderboard function context**
exec
/bin/zsh -lc "sed -n '740,860p' lib/api.ts" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
        actual_winner_id =
          (Array.isArray(game.home_team) ? game.home_team[0] : game.home_team)
            ?.id || null;
        gameResult = "HOME_WIN";
      } else if (game.away_score > game.home_score) {
        actual_winner_id =
          (Array.isArray(game.away_team) ? game.away_team[0] : game.away_team)
            ?.id || null;
        gameResult = "AWAY_WIN";
      }
    }

    // Get team names and ids safely
    const homeTeam = Array.isArray(game.home_team)
      ? game.home_team[0]
      : game.home_team;
    const awayTeam = Array.isArray(game.away_team)
      ? game.away_team[0]
      : game.away_team;


thinking
**Searching migrations for leaderboard logic**
exec
/bin/zsh -lc 'rg -n "get_leaderboard|leaderboard" supabase/migrations -S' in /Users/jeffrey/WebstormProjects/lookin/lookin exited 1 in 51ms:


thinking
**Planning migration additions**
exec
/bin/zsh -lc "sed -n '80,180p' prd.json" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
      "description": "\uae30\uc874 \uacbd\uae30 \uce74\ub4dc(\ud300 \uc120\ud0dd UI)\ub97c LMSR \ub9c8\ucf13 \uce74\ub4dc\ub85c \uad50\uccb4\ud55c\ub2e4. \uac01 \ub9c8\ucf13 \uce74\ub4dc\uc5d0 \uc2e4\uc2dc\uac04 \uac00\uaca9\uc774 \ud45c\uc2dc\ub418\uace0, \ud130\uce58\ud558\uba74 \uac70\ub798 \ud654\uba74\uc73c\ub85c \uc774\ub3d9\ud55c\ub2e4. \ub514\uc790\uc778\uc740 prd.md\uc758 W-03 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ubc0f \ub514\uc790\uc778 \ucca0\ud559/\ud1a0\ud070 \uc139\uc158 \ucc38\uc870.",
      "acceptanceCriteria": [
        "REQ-032 \uba54\uc778 \ud654\uba74\uc5d0 \uc624\ub298 \uacbd\uae30 \ub9c8\ucf13 \ubaa9\ub85d\uc744 \ud45c\uc2dc. \uac01 \uce74\ub4dc\uc5d0\ub294: \ud648\ud300/\uc6d0\uc815\ud300 \uc774\ub984, \uacbd\uae30\uc2dc\uac04, HOME/AWAY/DRAW \ud604\uc7ac \uac00\uaca9 (0~100), \ub9c8\ucf13 \uc0c1\ud0dc (OPEN/CLOSED/SETTLED). \ub514\uc790\uc778\uc740 prd.md W-03 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ub530\ub984.",
        "REQ-033 \uac00\uaca9 \ud45c\uc2dc\ub294 \uc18c\uc218\uc810 1\uc790\ub9ac\uae4c\uc9c0 (\uc608: 47.5). \uac00\uaca9 \uc0c9\uc0c1\uc740 prd.md \ub514\uc790\uc778 \ud1a0\ud070\uc758 \uae08\uc735 UI \uc0c9\uc0c1 \ub530\ub984 (\uc0c1\uc2b9=\ucd08\ub85d, \ud558\ub77d=\ube68\uac15).",
        "REQ-034 \uacbd\uae30 \uc0c1\ud0dc\uc5d0 \ub530\ub978 \ub9c8\ucf13 \uce74\ub4dc \ud45c\uc2dc: SCHEDULED \u2192 OPEN (\uac70\ub798 \uac00\ub2a5), IN_PROGRESS \u2192 OPEN (\uac70\ub798 \uac00\ub2a5), FINISHED \u2192 CLOSED (\uac70\ub798 \ubd88\uac00, \uacb0\uacfc \ub300\uae30), CANCELED \u2192 CANCELED.",
        "REQ-035 \ub9c8\ucf13 \uce74\ub4dc \ud130\uce58 \uc2dc /market/[id] \ud398\uc774\uc9c0\ub85c \uc774\ub3d9 (\ub9c8\ucf13 \uc0c1\uc138/\uac70\ub798 \ud654\uba74).",
        "REQ-036 PC\uc5d0\uc11c\ub3c4 430px \ubaa8\ubc14\uc77c \ud06c\uae30\ub85c \uc911\uc559 \uace0\uc815. \uc678\ubd80 \ubc30\uacbd #111111.",
        "REQ-037 \ubaa8\ub4e0 \uacbd\uae30\uac00 \uc885\ub8cc\ub41c \uacbd\uc6b0 \ub0b4\uc77c \uacbd\uae30 \ub9c8\ucf13\uc744 \ud45c\uc2dc (\uae30\uc874 \ub85c\uc9c1\uacfc \ub3d9\uc77c\ud55c \ud328\ud134).",
        "REQ-038 \uc0c1\ub2e8 \ud5e4\ub354: \uc88c\uce21 ToKHin' \ub85c\uace0, \uc6b0\uce21 \ucf54\uc778 \uc794\uace0 \ud45c\uc2dc."
      ]
    },
    {
      "id": "US-006",
      "title": "\ub9c8\ucf13 \uc0c1\uc138 & \uac70\ub798 \ud654\uba74 \u2014 \ub9e4\uc218/\ub9e4\ub3c4 \uc778\ud130\ud398\uc774\uc2a4",
      "priority": 6,
      "passes": true,
      "description": "\uac1c\ubcc4 \ub9c8\ucf13\uc758 \uc0c1\uc138 \uc815\ubcf4\uc640 \ub9e4\uc218/\ub9e4\ub3c4 \uc8fc\ubb38 \uc778\ud130\ud398\uc774\uc2a4\ub97c \uc81c\uacf5\ud558\ub294 \ud654\uba74\uc744 \uad6c\ud604\ud55c\ub2e4. \ub514\uc790\uc778\uc740 prd.md\uc758 W-04, W-05 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ubc0f \ub514\uc790\uc778 \ucca0\ud559/\ud1a0\ud070 \uc139\uc158 \ucc38\uc870.",
      "acceptanceCriteria": [
        "REQ-039 \ub9c8\ucf13 \uc0c1\uc138 \ud654\uba74 (/market/[id]) \uc0c1\ub2e8: \ud648\ud300 vs \uc6d0\uc815\ud300, \uacbd\uae30\uc2dc\uac04, \ub9c8\ucf13 \uc0c1\ud0dc, \ud604\uc7ac \uac00\uaca9 3\uac1c (HOME/AWAY/DRAW) \ud070 \uae00\uc528\ub85c \ud45c\uc2dc. \ud300 \uceec\ub7ec \ud65c\uc6a9.",
        "REQ-040 \ub0b4 \ud3ec\uc9c0\uc158 \uc139\uc158: \ud574\ub2f9 \ub9c8\ucf13\uc5d0\uc11c \ub0b4\uac00 \ubcf4\uc720\ud55c HOME/AWAY/DRAW \ud1a0\ud070 \uc218\ub7c9 + \ud604\uc7ac \ud3c9\uac00\uae08\uc561 (\uc218\ub7c9 \u00d7 \ud604\uc7ac\uac00) \ud45c\uc2dc. \ud3ec\uc9c0\uc158\uc774 \uc5c6\uc73c\uba74 '\ubcf4\uc720 \ud3ec\uc9c0\uc158 \uc5c6\uc74c' \ud45c\uc2dc.",

thinking
**Reviewing leaderboard wireframe details**
exec
/bin/zsh -lc 'rg -n "W-07|리더보드|잔고 순위|수익률 순위|leaderboard" prd.md' in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 51ms:
125:││ 마켓   포지션  리더보드 도움말 ││
222:### W-07: 리더보드
225:│  리더보드                    │
227:│  [잔고 순위] [수익률 순위]    │
290:  - REQ-008 모든 페이지 (메인, 히스토리, 리더보드, 튜토리얼)에서 로그인 여부 확인. 미로그인 시 로그인 화면으로 리다이렉트. Admin은 기존 비밀번호 방식 유지.
373:- Title: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
375:- Description: 리더보드를 코인 잔고 순위와 수익률 순위 두 가지 탭으로 제공한다. 와이어프레임 W-07 참조.
377:  - REQ-053 `get_leaderboard_balance() RETURNS TABLE(...)` RPC 함수. 코인 잔고 기준 내림차순 정렬. 반환: rank, user_id, username, balance.
378:  - REQ-054 `get_leaderboard_roi() RETURNS TABLE(...)` RPC 함수. 수익률 = (현재잔고 - 누적지급코인) / 누적지급코인 × 100. 반환: rank, user_id, username, roi_percent, current_balance, total_granted.
379:  - REQ-055 리더보드 UI에 "잔고 순위" / "수익률 순위" 탭 전환.
380:  - REQ-056 기존 리더보드 스타일 유지: 1등 tokhin-green 배경 + 흰 텍스트, 2~3등 tokhin-green 텍스트, 나머지 회색.
412:  - REQ-070 하단 네비게이션 메뉴 업데이트: 마켓(홈) / 포지션(히스토리) / 리더보드 / 도움말(튜토리얼). Admin은 기존처럼 별도 경로 `/admin`.

thinking
**Planning wireframe review**
exec
/bin/zsh -lc "sed -n '208,260p' prd.md" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
│                             │
│  ┌───────────────────────┐  │
│  │ LG vs 두산   10/15     │  │
│  │ AWAY 8주               │  │
│  │ 평균 43.2  현재 40.1    │  │
│  │ 미실현 손익: -25 코인 ▼  │  │
│  └───────────────────────┘  │
│                             │
│┌───────────────────────────┐│
││ 🏠    📊     🏆     ❓   ││
│└───────────────────────────┘│
└─────────────────────────────┘
```

### W-07: 리더보드
```
┌─────────────────────────────┐
│  리더보드                    │
│─────────────────────────────│
│  [잔고 순위] [수익률 순위]    │

thinking
**Inspecting migration naming pattern**
exec
/bin/zsh -lc "sed -n '1,260p' supabase/migrations/20260224_us007_history_positions.sql" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
-- US-007: 거래 히스토리 & 포지션 관리 화면 지원 RPC

-- REQ-049 지원: 진행 중 포지션 조회
CREATE OR REPLACE FUNCTION public.get_user_open_positions(
  p_user_id UUID
)
RETURNS TABLE(
  market_id INTEGER,
  outcome TEXT,
  quantity NUMERIC,
  avg_entry_price NUMERIC,
  updated_at TIMESTAMPTZ
)
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
BEGIN
  IF p_user_id IS NULL THEN
    RAISE EXCEPTION '사용자 정보가 올바르지 않습니다';

thinking
**Inspecting migration for indexes**
exec
/bin/zsh -lc "sed -n '1,360p' supabase/migrations/20260224_us004_wallet_system.sql" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
-- US-004: 지갑 시스템 - 코인 잔고 및 주간 자동 지급
-- 실행 대상: Supabase SQL Editor 또는 `supabase db execute`

-- REQ-027: 로그인 성공 시 wallet 자동 생성
CREATE OR REPLACE FUNCTION public.login(
  p_student_number BIGINT,
  p_password TEXT
)
RETURNS JSONB
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
DECLARE
  v_user RECORD;
  v_input_hash TEXT;
BEGIN
  SELECT id, username, password_hash, password_changed
  INTO v_user
  FROM public.users

thinking
**Inspecting transaction schema constraints**
exec
/bin/zsh -lc "sed -n '80,180p' supabase/migrations/20260224_us002_db_schema.sql" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
  outcome VARCHAR NOT NULL CHECK (outcome IN ('HOME', 'AWAY', 'DRAW')),
  side VARCHAR NOT NULL CHECK (side IN ('BUY', 'SELL')),
  quantity NUMERIC NOT NULL CHECK (quantity > 0),
  total_cost NUMERIC NOT NULL,
  avg_price NUMERIC NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- REQ-014 positions
CREATE TABLE IF NOT EXISTS public.positions (
  id SERIAL PRIMARY KEY,
  user_id UUID NOT NULL REFERENCES public.users(id) ON DELETE CASCADE,
  market_id INTEGER NOT NULL REFERENCES public.markets(id) ON DELETE CASCADE,
  outcome VARCHAR NOT NULL CHECK (outcome IN ('HOME', 'AWAY', 'DRAW')),
  quantity NUMERIC NOT NULL DEFAULT 0,
  avg_entry_price NUMERIC NOT NULL DEFAULT 0,
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  CONSTRAINT positions_user_market_outcome_key UNIQUE (user_id, market_id, outcome)
);


thinking
**Reviewing history tab UI patterns**
exec
/bin/zsh -lc "sed -n '1,260p' app/history/page.tsx" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
"use client";

import { useEffect, useMemo, useRef, useState } from "react";
import {
  getOpenPositionsHistory,
  getOrderHistoryByDate,
  getSettlementHistory,
  type OpenPositionHistoryItem,
  type OrderHistoryItem,
  type SettlementHistoryItem,
} from "@/lib/api";
import { useUserSession } from "@/lib/hooks/useUserSession";

type HistoryTab = "OPEN_POSITIONS" | "ORDERS" | "SETTLEMENTS";

const KST_OFFSET_MINUTES = 9 * 60;
const HISTORY_TABS: Array<{ id: HistoryTab; label: string }> = [
  { id: "OPEN_POSITIONS", label: "진행 중 포지션" },
  { id: "ORDERS", label: "거래 내역" },
  { id: "SETTLEMENTS", label: "정산 내역" },

thinking
**Inspecting header for tab styles**
exec
/bin/zsh -lc 'rg -n "HISTORY_TABS|activeTab|border-b|underline|탭|button" app/history/page.tsx' in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
17:const HISTORY_TABS: Array<{ id: HistoryTab; label: string }> = [
199:  const [activeTab, setActiveTab] = useState<HistoryTab>("OPEN_POSITIONS");
443:      <div className="mb-4 border-b border-zinc-200">
445:          {HISTORY_TABS.map((tab) => (
446:            <button
448:              type="button"
451:                activeTab === tab.id
452:                  ? "border-b-2 border-black text-black"
457:            </button>
462:      {activeTab === "ORDERS" && (
465:            <button
466:              type="button"
472:            </button>
475:              <button
476:                type="button"
481:              </button>
513:                          <button
514:                            type="button"
526:                          </button>
537:            <button
exec
/bin/zsh -lc "sed -n '420,520p' app/history/page.tsx" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 51ms:
    return days;
  };

  const handleCalendarDateSelect = (date: Date) => {
    const localDate = new Date(date.getFullYear(), date.getMonth(), date.getDate());
    setSelectedDate(formatDate(localDate));
    setShowCalendar(false);
  };

  if (isSessionLoading) {
    return <p className="py-20 text-center text-zinc-500">로딩 중...</p>;
  }

  if (!session?.user_id) {
    return null;
  }

  return (
    <div className="w-full pb-4">
      <h1 className="mb-4 text-center text-xl font-bold text-black">

thinking
**Assessing leaderboard styling and API integration**
codex
`US-008`은 기존 점수제 `get_leaderboard`를 새 RPC 2개와 탭 UI로 교체하는 작업입니다. 이제 `lib/api.ts` 하단 구조를 확인해 타입/함수 추가 위치를 정하고, `supabase/migrations`에 `US-008` SQL을 신규로 추가하겠습니다.
exec
/bin/zsh -lc "sed -n '1500,1820p' lib/api.ts" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 51ms:
    items.push({
      marketId,
      settledAt: row.settled_at,
      settlementAmount: toNumberOrNull(row.settlement_amount) ?? 0,
      netInvested: toNumberOrNull(row.net_invested) ?? 0,
      finalPnl: toNumberOrNull(row.final_pnl) ?? 0,
      result,
      positions: {
        HOME: toNumberOrNull(row.home_quantity) ?? 0,
        AWAY: toNumberOrNull(row.away_quantity) ?? 0,
        DRAW: toNumberOrNull(row.draw_quantity) ?? 0,
      },
      gameDate: game.game_date,
      gameTime: game.game_time ?? null,
      homeTeamName: homeTeam.name,
      awayTeamName: awayTeam.name,
      homeTeamShortName: homeTeam.short_name ?? null,
      awayTeamShortName: awayTeam.short_name ?? null,
    });
  });

thinking
**Identifying leaderboard function usages**
exec
/bin/zsh -lc "rg -n \"getLeaderboard\\(|get_leaderboard\\b|leaderboard\" app lib -S" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 51ms:
lib/api.ts:785:  const { data, error } = await supabase.rpc("get_leaderboard", {
lib/api.ts:791:    console.error("Error fetching leaderboard:", error);
app/leaderboard/page.tsx:20:  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
app/leaderboard/page.tsx:28:      const data = await getLeaderboard("2025-10-05", "2025-12-31");
app/leaderboard/page.tsx:55:      ) : leaderboard.length === 0 ? (
app/leaderboard/page.tsx:62:            {leaderboard.map((entry, index) => (

thinking
**Planning leaderboard API update**
exec
/bin/zsh -lc 'cat package.json' in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
{
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@radix-ui/react-checkbox": "^1.3.1",
    "@radix-ui/react-dropdown-menu": "^2.1.15",
    "@radix-ui/react-label": "^2.1.6",
    "@radix-ui/react-slot": "^1.2.2",
    "@supabase/ssr": "latest",
    "@supabase/supabase-js": "latest",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.511.0",
    "next": "latest",
    "next-themes": "^0.4.6",

thinking
**Inspecting migration and UI styles**
exec
/bin/zsh -lc "sed -n '1,240p' app/layout.tsx" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 51ms:
import type { Metadata, Viewport } from "next";
import { ThemeProvider } from "next-themes";
import Navigation from "@/components/navigation"; // Import the new component
import AppHeader from "@/components/app-header";
import "./globals.css";
import localFont from "next/font/local";

const defaultUrl = process.env.VERCEL_PROJECT_PRODUCTION_URL
  ? `https://${process.env.VERCEL_PROJECT_PRODUCTION_URL}`
  : "http://localhost:3000";

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export const metadata: Metadata = {
  metadataBase: new URL(defaultUrl),
  title: "ToKHin' 승부 예측",
  icons: {

thinking
**Inspecting layout and tsconfig settings**
exec
/bin/zsh -lc 'cat tsconfig.json' in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 51ms:
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }

thinking
**Planning skill usage announcement**
codex
적용 스킬: `brainstorming`을 최소 범위로 적용해 요구사항을 설계로 확정했고, `test-driven-development` 원칙에 맞춰 먼저 SQL/API/UI 변경 범위를 검증 가능한 단위로 나눈 뒤 구현하겠습니다.

thinking
**Planning multi-step implementation**
Plan update
US-008 requires DB RPC additions, API surface updates, and leaderboard UI tab behavior while preserving styling rules.
  → Inspect current leaderboard data flow and identify exact files/migrations to update for US-008.
  • Add Supabase migration for REQ-053/REQ-054 RPC functions and grants.
  • Update lib/api.ts with new leaderboard RPC client functions and types.
  • Refactor app/leaderboard/page.tsx to support balance/ROI tab switching with required ranking styles.
  • Run verification (lint/build subset) and update prd.json story status for US-008 if all criteria are met.

thinking
**Planning leaderboard ROI logic**
codex
이제 파일을 수정합니다. 먼저 `supabase/migrations`에 `US-008` 전용 SQL을 추가해 `get_leaderboard_balance`, `get_leaderboard_roi` RPC와 권한 부여를 구현하겠습니다. ત્યાર પછી `lib/api.ts`와 `app/leaderboard/page.tsx`를 새 RPC 기준으로 교체하겠습니다.
file update
A /Users/jeffrey/WebstormProjects/lookin/lookin/supabase/migrations/20260224_us008_leaderboard_rankings.sql
-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹

-- REQ-053: 코인 잔고 순위 조회
CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
RETURNS TABLE(
  rank BIGINT,
  user_id UUID,
  username TEXT,
  balance NUMERIC
)
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
BEGIN
  RETURN QUERY
  WITH leaderboard AS (
    SELECT
      u.id AS user_id,
      u.username,
      COALESCE(w.balance, 0) AS balance
    FROM public.users u
    LEFT JOIN public.wallets w
      ON w.user_id = u.id
  ),
  ranked AS (
    SELECT
      RANK() OVER (
        ORDER BY
          l.balance DESC,
          l.username ASC,
          l.user_id ASC
      )::BIGINT AS position_rank,
      l.user_id,
      l.username,
      l.balance
    FROM leaderboard l
  )
  SELECT
    r.position_rank AS rank,
    r.user_id,
    r.username,
    r.balance
  FROM ranked r
  ORDER BY
    r.position_rank ASC,
    r.username ASC,
    r.user_id ASC;
END;
$$;

-- REQ-054: 수익률 순위 조회
CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
RETURNS TABLE(
  rank BIGINT,
  user_id UUID,
  username TEXT,
  roi_percent NUMERIC,
  current_balance NUMERIC,
  total_granted NUMERIC
)
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
BEGIN
  RETURN QUERY
  WITH granted AS (
    SELECT
      t.user_id,
      COALESCE(SUM(t.amount), 0) AS total_granted
    FROM public.transactions t
    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
    GROUP BY t.user_id
  ),
  roi_base AS (
    SELECT
      u.id AS user_id,
      u.username,
      COALESCE(w.balance, 0) AS current_balance,
      COALESCE(g.total_granted, 0) AS total_granted
    FROM public.users u
    LEFT JOIN public.wallets w
      ON w.user_id = u.id
    LEFT JOIN granted g
      ON g.user_id = u.id
  ),
  scored AS (
    SELECT
      rb.user_id,
      rb.username,
      rb.current_balance,
      rb.total_granted,
      CASE
        WHEN rb.total_granted > 0
          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
        ELSE 0
      END AS roi_percent
    FROM roi_base rb
  ),
  ranked AS (
    SELECT
      RANK() OVER (
        ORDER BY
          s.roi_percent DESC,
          s.current_balance DESC,
          s.username ASC,
          s.user_id ASC
      )::BIGINT AS position_rank,
      s.user_id,
      s.username,
      s.roi_percent,
      s.current_balance,
      s.total_granted
    FROM scored s
  )
  SELECT
    r.position_rank AS rank,
    r.user_id,
    r.username,
    r.roi_percent,
    r.current_balance,
    r.total_granted
  FROM ranked r
  ORDER BY
    r.position_rank ASC,
    r.username ASC,
    r.user_id ASC;
END;
$$;

GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;
apply_patch(auto_approved=true) exited 0 in 44ms:
Success. Updated the following files:
A supabase/migrations/20260224_us008_leaderboard_rankings.sql
file update:
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;

file update:
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Inspecting interfaces for patching**
exec
/bin/zsh -lc "sed -n '80,240p' lib/api.ts" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 51ms:

interface LmsrPriceRow {
  outcome: string;
  price: number | string;
}

interface MarketDetailRpcPayload {
  market?: {
    id: number | string;
    game_id: number | string;
    q_home: number | string;
    q_away: number | string;
    q_draw: number | string;
    b: number | string;
    status: string;
    result: string | null;
    initial_home_price: number | string | null;
    initial_away_price: number | string | null;
    initial_draw_price: number | string | null;
  };
file update:
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Planning interface insertion point**
exec
/bin/zsh -lc "sed -n '240,340p' lib/api.ts" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
  orderId: number;
  marketId: number;
  outcome: MarketOutcome;
  side: "BUY" | "SELL";
  quantity: number;
  totalAmount: number;
  avgPrice: number;
  createdAt: string;
  gameDate: string;
  gameTime: string | null;
  homeTeamName: string;
  awayTeamName: string;
  homeTeamShortName: string | null;
  awayTeamShortName: string | null;
}

export interface SettlementHistoryItem {
  marketId: number;
  settledAt: string;
  settlementAmount: number;
file update:
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Planning leaderboard interface insertion**
exec
/bin/zsh -lc "sed -n '760,840p' lib/api.ts" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 51ms:
    return {
      ...game,
      gameResult,
      prediction: prediction
        ? {
            predicted_team_name:
              prediction.predicted_winner_team_id === homeTeam.id
                ? homeTeam.name
                : awayTeam.name,
            is_correct:
              actual_winner_id !== null
                ? prediction.predicted_winner_team_id === actual_winner_id
                : false,
            points_earned: prediction.points_earned || 0,
            is_settled: prediction.is_settled || false,
          }
        : null,
    };
  });

file update:
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Applying interface type patches**
file update
M /Users/jeffrey/WebstormProjects/lookin/lookin/lib/api.ts
@@ -172,2 +172,18 @@
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
@@ -271,2 +287,18 @@
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
@@ -782,27 +814,41 @@
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
apply_patch(auto_approved=true) exited 0 in 38ms:
Success. Updated the following files:
M lib/api.ts
file update:
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;

file update:
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Planning full file rewrite**
exec
/bin/zsh -lc "sed -n '1,260p' app/leaderboard/page.tsx" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
"use client";

import { useState, useEffect } from "react";
import { getLeaderboard } from "@/lib/api";
import { useIsMobile } from "@/lib/hooks/useResponsive";
import { useUserSession } from "@/lib/hooks/useUserSession";

type LeaderboardEntry = {
  userId: string;
  name: string;
  student_number: string;
  score: number;
};

export default function LeaderboardPage() {
  const isMobile = useIsMobile();
  const { session, isLoading: isSessionLoading } = useUserSession({
    requireAuth: true,
  });
  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
file update:
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Rewriting leaderboard component logic**
file update
M /Users/jeffrey/WebstormProjects/lookin/lookin/app/leaderboard/page.tsx
@@ -2,4 +2,9 @@
 
-import { useState, useEffect } from "react";
-import { getLeaderboard } from "@/lib/api";
+import { useEffect, useMemo, useState } from "react";
+import {
+  getLeaderboardBalance,
+  getLeaderboardRoi,
+  type LeaderboardBalanceItem,
+  type LeaderboardRoiItem,
+} from "@/lib/api";
 import { useIsMobile } from "@/lib/hooks/useResponsive";
@@ -7,7 +12,39 @@
 
-type LeaderboardEntry = {
-  userId: string;
-  name: string;
-  student_number: string;
-  score: number;
+type LeaderboardTab = "BALANCE" | "ROI";
+
+type LeaderboardDisplayRow = {
+  key: string;
+  rank: number;
+  username: string;
+  valueText: string;
+};
+
+const LEADERBOARD_TABS: Array<{ id: LeaderboardTab; label: string }> = [
+  { id: "BALANCE", label: "잔고 순위" },
+  { id: "ROI", label: "수익률 순위" },
+];
+
+const valueFormatter = new Intl.NumberFormat("ko-KR", {
+  maximumFractionDigits: 2,
+});
+
+const formatRoiPercent = (value: number) => {
+  const formatted = valueFormatter.format(Math.abs(value));
+  return `${value >= 0 ? "+" : "-"}${formatted}%`;
+};
+
+const getRankTextClass = (rank: number) => {
+  if (rank === 1) {
+    return "text-white";
+  }
+
+  if (rank === 2 || rank === 3) {
+    return "text-tokhin-green";
+  }
+
+  return "text-stone-500";
+};
+
+const getRankBackgroundClass = (rank: number) => {
+  return rank === 1 ? "bg-tokhin-green" : "bg-neutral-50";
 };
@@ -19,17 +56,78 @@
   });
-  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
+
+  const [activeTab, setActiveTab] = useState<LeaderboardTab>("BALANCE");
+  const [balanceLeaderboard, setBalanceLeaderboard] = useState<
+    LeaderboardBalanceItem[]
+  >([]);
+  const [roiLeaderboard, setRoiLeaderboard] = useState<LeaderboardRoiItem[]>([]);
   const [isLoading, setIsLoading] = useState(true);
+  const [errorMessage, setErrorMessage] = useState<string | null>(null);
 
   useEffect(() => {
-    if (!session) return;
+    if (!session?.user_id) {
+      return;
+    }
 
+    let isCancelled = false;
     const fetchLeaderboard = async () => {
       setIsLoading(true);
-      const data = await getLeaderboard("2025-10-05", "2025-12-31");
-      setLeaderboard(data);
-      setIsLoading(false);
+      setErrorMessage(null);
+
+      try {
+        const [balanceData, roiData] = await Promise.all([
+          getLeaderboardBalance(),
+          getLeaderboardRoi(),
+        ]);
+
+        if (isCancelled) {
+          return;
+        }
+
+        setBalanceLeaderboard(balanceData);
+        setRoiLeaderboard(roiData);
+      } catch (error) {
+        if (isCancelled) {
+          return;
+        }
+
+        const message =
+          error instanceof Error
+            ? error.message
+            : "리더보드 조회 중 오류가 발생했습니다";
+
+        setErrorMessage(message);
+        setBalanceLeaderboard([]);
+        setRoiLeaderboard([]);
+      } finally {
+        if (!isCancelled) {
+          setIsLoading(false);
+        }
+      }
     };
+
     fetchLeaderboard();
-  }, [session]);
 
+    return () => {
+      isCancelled = true;
+    };
+  }, [session?.user_id]);
+
+  const displayRows = useMemo<LeaderboardDisplayRow[]>(() => {
+    if (activeTab === "BALANCE") {
+      return balanceLeaderboard.map((entry) => ({
+        key: `balance-${entry.userId}`,
+        rank: entry.rank,
+        username: entry.username,
+        valueText: valueFormatter.format(entry.balance),
+      }));
+    }
+
+    return roiLeaderboard.map((entry) => ({
+      key: `roi-${entry.userId}`,
+      rank: entry.rank,
+      username: entry.username,
+      valueText: formatRoiPercent(entry.roiPercent),
+    }));
+  }, [activeTab, balanceLeaderboard, roiLeaderboard]);
+
   if (isSessionLoading) {
@@ -49,10 +147,32 @@
       >
-        실시간 순위
+        리더보드
       </h1>
 
+      <div className="mb-5 border-b border-zinc-200">
+        <div className="grid grid-cols-2">
+          {LEADERBOARD_TABS.map((tab) => (
+            <button
+              key={tab.id}
+              type="button"
+              onClick={() => setActiveTab(tab.id)}
+              className={`pb-3 text-sm font-semibold transition-colors ${
+                activeTab === tab.id
+                  ? "border-b-2 border-black text-black"
+                  : "text-zinc-400"
+              }`}
+            >
+              {tab.label}
+            </button>
+          ))}
+        </div>
+      </div>
+
       {isLoading ? (
         <p className="text-center">로딩 중...</p>
-      ) : leaderboard.length === 0 ? (
+      ) : errorMessage ? (
+        <p className="text-center text-red-500">{errorMessage}</p>
+      ) : displayRows.length === 0 ? (
         <p className="text-center text-gray-600">
-          아직 순위표를 볼 수 없습니다.
+          아직 {activeTab === "BALANCE" ? "잔고" : "수익률"} 순위표를 볼 수
+          없습니다.
         </p>
@@ -61,19 +181,13 @@
           <ul className="divide-y divide-transparent">
-            {leaderboard.map((entry, index) => (
+            {displayRows.map((entry) => (
               <li
-                key={entry.userId}
+                key={entry.key}
                 className={`flex items-center ${
                   isMobile ? "px-4" : "px-4"
-                } h-12 ${
-                  index === 0 ? "bg-tokhin-green" : "bg-neutral-50"
-                } rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
+                } h-12 ${getRankBackgroundClass(
+                  entry.rank
+                )} rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
               >
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${
+                  className={`font-base ${getRankTextClass(entry.rank)} ${
                     isMobile
@@ -83,25 +197,17 @@
                 >
-                  {index + 1}
+                  {entry.rank}
                 </span>
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
+                  className={`font-base ${getRankTextClass(
+                    entry.rank
+                  )} flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
                 >
-                  {entry.name}
+                  {entry.username}
                 </span>
                 <span
-                  className={`font-normal ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${isMobile ? "text-base px-1" : "text-2xl"}`}
+                  className={`font-normal ${getRankTextClass(entry.rank)} ${
+                    isMobile ? "text-base px-1" : "text-2xl"
+                  }`}
                 >
-                  {entry.score}
+                  {entry.valueText}
                 </span>
apply_patch(auto_approved=true) exited 0 in 49ms:
Success. Updated the following files:
M app/leaderboard/page.tsx
file update:
diff --git a/app/leaderboard/page.tsx b/app/leaderboard/page.tsx
index 2adb2b80e2f8fed380614a6cbd203a475e0a18fa..cf6731cc7b5e350119555e4e2824b14c9a2f3f56
--- a/app/leaderboard/page.tsx
+++ b/app/leaderboard/page.tsx
@@ -1,15 +1,52 @@
 "use client";
 
-import { useState, useEffect } from "react";
-import { getLeaderboard } from "@/lib/api";
+import { useEffect, useMemo, useState } from "react";
+import {
+  getLeaderboardBalance,
+  getLeaderboardRoi,
+  type LeaderboardBalanceItem,
+  type LeaderboardRoiItem,
+} from "@/lib/api";
 import { useIsMobile } from "@/lib/hooks/useResponsive";
 import { useUserSession } from "@/lib/hooks/useUserSession";
 
-type LeaderboardEntry = {
-  userId: string;
-  name: string;
-  student_number: string;
-  score: number;
+type LeaderboardTab = "BALANCE" | "ROI";
+
+type LeaderboardDisplayRow = {
+  key: string;
+  rank: number;
+  username: string;
+  valueText: string;
+};
+
+const LEADERBOARD_TABS: Array<{ id: LeaderboardTab; label: string }> = [
+  { id: "BALANCE", label: "잔고 순위" },
+  { id: "ROI", label: "수익률 순위" },
+];
+
+const valueFormatter = new Intl.NumberFormat("ko-KR", {
+  maximumFractionDigits: 2,
+});
+
+const formatRoiPercent = (value: number) => {
+  const formatted = valueFormatter.format(Math.abs(value));
+  return `${value >= 0 ? "+" : "-"}${formatted}%`;
+};
+
+const getRankTextClass = (rank: number) => {
+  if (rank === 1) {
+    return "text-white";
+  }
+
+  if (rank === 2 || rank === 3) {
+    return "text-tokhin-green";
+  }
+
+  return "text-stone-500";
+};
+
+const getRankBackgroundClass = (rank: number) => {
+  return rank === 1 ? "bg-tokhin-green" : "bg-neutral-50";
 };
 
 export default function LeaderboardPage() {
@@ -17,21 +54,82 @@
   const { session, isLoading: isSessionLoading } = useUserSession({
     requireAuth: true,
   });
-  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
+
+  const [activeTab, setActiveTab] = useState<LeaderboardTab>("BALANCE");
+  const [balanceLeaderboard, setBalanceLeaderboard] = useState<
+    LeaderboardBalanceItem[]
+  >([]);
+  const [roiLeaderboard, setRoiLeaderboard] = useState<LeaderboardRoiItem[]>([]);
   const [isLoading, setIsLoading] = useState(true);
+  const [errorMessage, setErrorMessage] = useState<string | null>(null);
 
   useEffect(() => {
-    if (!session) return;
+    if (!session?.user_id) {
+      return;
+    }
 
+    let isCancelled = false;
     const fetchLeaderboard = async () => {
       setIsLoading(true);
-      const data = await getLeaderboard("2025-10-05", "2025-12-31");
-      setLeaderboard(data);
-      setIsLoading(false);
+      setErrorMessage(null);
+
+      try {
+        const [balanceData, roiData] = await Promise.all([
+          getLeaderboardBalance(),
+          getLeaderboardRoi(),
+        ]);
+
+        if (isCancelled) {
+          return;
+        }
+
+        setBalanceLeaderboard(balanceData);
+        setRoiLeaderboard(roiData);
+      } catch (error) {
+        if (isCancelled) {
+          return;
+        }
+
+        const message =
+          error instanceof Error
+            ? error.message
+            : "리더보드 조회 중 오류가 발생했습니다";
+
+        setErrorMessage(message);
+        setBalanceLeaderboard([]);
+        setRoiLeaderboard([]);
+      } finally {
+        if (!isCancelled) {
+          setIsLoading(false);
+        }
+      }
     };
+
     fetchLeaderboard();
-  }, [session]);
 
+    return () => {
+      isCancelled = true;
+    };
+  }, [session?.user_id]);
+
+  const displayRows = useMemo<LeaderboardDisplayRow[]>(() => {
+    if (activeTab === "BALANCE") {
+      return balanceLeaderboard.map((entry) => ({
+        key: `balance-${entry.userId}`,
+        rank: entry.rank,
+        username: entry.username,
+        valueText: valueFormatter.format(entry.balance),
+      }));
+    }
+
+    return roiLeaderboard.map((entry) => ({
+      key: `roi-${entry.userId}`,
+      rank: entry.rank,
+      username: entry.username,
+      valueText: formatRoiPercent(entry.roiPercent),
+    }));
+  }, [activeTab, balanceLeaderboard, roiLeaderboard]);
+
   if (isSessionLoading) {
     return <p className="py-20 text-center text-zinc-500">로딩 중...</p>;
   }
@@ -47,63 +145,71 @@
           isMobile ? "text-xl" : "text-4xl"
         }`}
       >
-        실시간 순위
+        리더보드
       </h1>
 
+      <div className="mb-5 border-b border-zinc-200">
+        <div className="grid grid-cols-2">
+          {LEADERBOARD_TABS.map((tab) => (
+            <button
+              key={tab.id}
+              type="button"
+              onClick={() => setActiveTab(tab.id)}
+              className={`pb-3 text-sm font-semibold transition-colors ${
+                activeTab === tab.id
+                  ? "border-b-2 border-black text-black"
+                  : "text-zinc-400"
+              }`}
+            >
+              {tab.label}
+            </button>
+          ))}
+        </div>
+      </div>
+
       {isLoading ? (
         <p className="text-center">로딩 중...</p>
-      ) : leaderboard.length === 0 ? (
+      ) : errorMessage ? (
+        <p className="text-center text-red-500">{errorMessage}</p>
+      ) : displayRows.length === 0 ? (
         <p className="text-center text-gray-600">
-          아직 순위표를 볼 수 없습니다.
+          아직 {activeTab === "BALANCE" ? "잔고" : "수익률"} 순위표를 볼 수
+          없습니다.
         </p>
       ) : (
         <div>
           <ul className="divide-y divide-transparent">
-            {leaderboard.map((entry, index) => (
+            {displayRows.map((entry) => (
               <li
-                key={entry.userId}
+                key={entry.key}
                 className={`flex items-center ${
                   isMobile ? "px-4" : "px-4"
-                } h-12 ${
-                  index === 0 ? "bg-tokhin-green" : "bg-neutral-50"
-                } rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
+                } h-12 ${getRankBackgroundClass(
+                  entry.rank
+                )} rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
               >
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${
+                  className={`font-base ${getRankTextClass(entry.rank)} ${
                     isMobile
                       ? "text-base w-6 text-center"
                       : "text-lg w-12 text-center"
                   }`}
                 >
-                  {index + 1}
+                  {entry.rank}
                 </span>
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
+                  className={`font-base ${getRankTextClass(
+                    entry.rank
+                  )} flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
                 >
-                  {entry.name}
+                  {entry.username}
                 </span>
                 <span
-                  className={`font-normal ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${isMobile ? "text-base px-1" : "text-2xl"}`}
+                  className={`font-normal ${getRankTextClass(entry.rank)} ${
+                    isMobile ? "text-base px-1" : "text-2xl"
+                  }`}
                 >
-                  {entry.score}
+                  {entry.valueText}
                 </span>
               </li>
             ))}
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;

file update:
diff --git a/app/leaderboard/page.tsx b/app/leaderboard/page.tsx
index 2adb2b80e2f8fed380614a6cbd203a475e0a18fa..cf6731cc7b5e350119555e4e2824b14c9a2f3f56
--- a/app/leaderboard/page.tsx
+++ b/app/leaderboard/page.tsx
@@ -1,15 +1,52 @@
 "use client";
 
-import { useState, useEffect } from "react";
-import { getLeaderboard } from "@/lib/api";
+import { useEffect, useMemo, useState } from "react";
+import {
+  getLeaderboardBalance,
+  getLeaderboardRoi,
+  type LeaderboardBalanceItem,
+  type LeaderboardRoiItem,
+} from "@/lib/api";
 import { useIsMobile } from "@/lib/hooks/useResponsive";
 import { useUserSession } from "@/lib/hooks/useUserSession";
 
-type LeaderboardEntry = {
-  userId: string;
-  name: string;
-  student_number: string;
-  score: number;
+type LeaderboardTab = "BALANCE" | "ROI";
+
+type LeaderboardDisplayRow = {
+  key: string;
+  rank: number;
+  username: string;
+  valueText: string;
+};
+
+const LEADERBOARD_TABS: Array<{ id: LeaderboardTab; label: string }> = [
+  { id: "BALANCE", label: "잔고 순위" },
+  { id: "ROI", label: "수익률 순위" },
+];
+
+const valueFormatter = new Intl.NumberFormat("ko-KR", {
+  maximumFractionDigits: 2,
+});
+
+const formatRoiPercent = (value: number) => {
+  const formatted = valueFormatter.format(Math.abs(value));
+  return `${value >= 0 ? "+" : "-"}${formatted}%`;
+};
+
+const getRankTextClass = (rank: number) => {
+  if (rank === 1) {
+    return "text-white";
+  }
+
+  if (rank === 2 || rank === 3) {
+    return "text-tokhin-green";
+  }
+
+  return "text-stone-500";
+};
+
+const getRankBackgroundClass = (rank: number) => {
+  return rank === 1 ? "bg-tokhin-green" : "bg-neutral-50";
 };
 
 export default function LeaderboardPage() {
@@ -17,21 +54,82 @@
   const { session, isLoading: isSessionLoading } = useUserSession({
     requireAuth: true,
   });
-  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
+
+  const [activeTab, setActiveTab] = useState<LeaderboardTab>("BALANCE");
+  const [balanceLeaderboard, setBalanceLeaderboard] = useState<
+    LeaderboardBalanceItem[]
+  >([]);
+  const [roiLeaderboard, setRoiLeaderboard] = useState<LeaderboardRoiItem[]>([]);
   const [isLoading, setIsLoading] = useState(true);
+  const [errorMessage, setErrorMessage] = useState<string | null>(null);
 
   useEffect(() => {
-    if (!session) return;
+    if (!session?.user_id) {
+      return;
+    }
 
+    let isCancelled = false;
     const fetchLeaderboard = async () => {
       setIsLoading(true);
-      const data = await getLeaderboard("2025-10-05", "2025-12-31");
-      setLeaderboard(data);
-      setIsLoading(false);
+      setErrorMessage(null);
+
+      try {
+        const [balanceData, roiData] = await Promise.all([
+          getLeaderboardBalance(),
+          getLeaderboardRoi(),
+        ]);
+
+        if (isCancelled) {
+          return;
+        }
+
+        setBalanceLeaderboard(balanceData);
+        setRoiLeaderboard(roiData);
+      } catch (error) {
+        if (isCancelled) {
+          return;
+        }
+
+        const message =
+          error instanceof Error
+            ? error.message
+            : "리더보드 조회 중 오류가 발생했습니다";
+
+        setErrorMessage(message);
+        setBalanceLeaderboard([]);
+        setRoiLeaderboard([]);
+      } finally {
+        if (!isCancelled) {
+          setIsLoading(false);
+        }
+      }
     };
+
     fetchLeaderboard();
-  }, [session]);
 
+    return () => {
+      isCancelled = true;
+    };
+  }, [session?.user_id]);
+
+  const displayRows = useMemo<LeaderboardDisplayRow[]>(() => {
+    if (activeTab === "BALANCE") {
+      return balanceLeaderboard.map((entry) => ({
+        key: `balance-${entry.userId}`,
+        rank: entry.rank,
+        username: entry.username,
+        valueText: valueFormatter.format(entry.balance),
+      }));
+    }
+
+    return roiLeaderboard.map((entry) => ({
+      key: `roi-${entry.userId}`,
+      rank: entry.rank,
+      username: entry.username,
+      valueText: formatRoiPercent(entry.roiPercent),
+    }));
+  }, [activeTab, balanceLeaderboard, roiLeaderboard]);
+
   if (isSessionLoading) {
     return <p className="py-20 text-center text-zinc-500">로딩 중...</p>;
   }
@@ -47,63 +145,71 @@
           isMobile ? "text-xl" : "text-4xl"
         }`}
       >
-        실시간 순위
+        리더보드
       </h1>
 
+      <div className="mb-5 border-b border-zinc-200">
+        <div className="grid grid-cols-2">
+          {LEADERBOARD_TABS.map((tab) => (
+            <button
+              key={tab.id}
+              type="button"
+              onClick={() => setActiveTab(tab.id)}
+              className={`pb-3 text-sm font-semibold transition-colors ${
+                activeTab === tab.id
+                  ? "border-b-2 border-black text-black"
+                  : "text-zinc-400"
+              }`}
+            >
+              {tab.label}
+            </button>
+          ))}
+        </div>
+      </div>
+
       {isLoading ? (
         <p className="text-center">로딩 중...</p>
-      ) : leaderboard.length === 0 ? (
+      ) : errorMessage ? (
+        <p className="text-center text-red-500">{errorMessage}</p>
+      ) : displayRows.length === 0 ? (
         <p className="text-center text-gray-600">
-          아직 순위표를 볼 수 없습니다.
+          아직 {activeTab === "BALANCE" ? "잔고" : "수익률"} 순위표를 볼 수
+          없습니다.
         </p>
       ) : (
         <div>
           <ul className="divide-y divide-transparent">
-            {leaderboard.map((entry, index) => (
+            {displayRows.map((entry) => (
               <li
-                key={entry.userId}
+                key={entry.key}
                 className={`flex items-center ${
                   isMobile ? "px-4" : "px-4"
-                } h-12 ${
-                  index === 0 ? "bg-tokhin-green" : "bg-neutral-50"
-                } rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
+                } h-12 ${getRankBackgroundClass(
+                  entry.rank
+                )} rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
               >
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${
+                  className={`font-base ${getRankTextClass(entry.rank)} ${
                     isMobile
                       ? "text-base w-6 text-center"
                       : "text-lg w-12 text-center"
                   }`}
                 >
-                  {index + 1}
+                  {entry.rank}
                 </span>
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
+                  className={`font-base ${getRankTextClass(
+                    entry.rank
+                  )} flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
                 >
-                  {entry.name}
+                  {entry.username}
                 </span>
                 <span
-                  className={`font-normal ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${isMobile ? "text-base px-1" : "text-2xl"}`}
+                  className={`font-normal ${getRankTextClass(entry.rank)} ${
+                    isMobile ? "text-base px-1" : "text-2xl"
+                  }`}
                 >
-                  {entry.score}
+                  {entry.valueText}
                 </span>
               </li>
             ))}
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Planning patch for line break check**
codex
DB/API/UI 핵심 변경을 반영했습니다. 이어서 `prd.json`의 `US-008` 상태를 `passes: true`로 업데이트하고, 타입/린트 검증을 실행해 회귀 여부를 확인하겠습니다.
file update
M /Users/jeffrey/WebstormProjects/lookin/lookin/prd.json
@@ -126,3 +126,3 @@
       "priority": 8,
-      "passes": false,
+      "passes": true,
       "description": "\ub9ac\ub354\ubcf4\ub4dc\ub97c \ucf54\uc778 \uc794\uace0 \uc21c\uc704\uc640 \uc218\uc775\ub960 \uc21c\uc704 \ub450 \uac00\uc9c0 \ud0ed\uc73c\ub85c \uc81c\uacf5\ud55c\ub2e4. \ub514\uc790\uc778\uc740 prd.md\uc758 W-07 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ucc38\uc870.",
apply_patch(auto_approved=true) exited 0 in 49ms:
Success. Updated the following files:
M prd.json
file update:
diff --git a/app/leaderboard/page.tsx b/app/leaderboard/page.tsx
index 2adb2b80e2f8fed380614a6cbd203a475e0a18fa..cf6731cc7b5e350119555e4e2824b14c9a2f3f56
--- a/app/leaderboard/page.tsx
+++ b/app/leaderboard/page.tsx
@@ -1,15 +1,52 @@
 "use client";
 
-import { useState, useEffect } from "react";
-import { getLeaderboard } from "@/lib/api";
+import { useEffect, useMemo, useState } from "react";
+import {
+  getLeaderboardBalance,
+  getLeaderboardRoi,
+  type LeaderboardBalanceItem,
+  type LeaderboardRoiItem,
+} from "@/lib/api";
 import { useIsMobile } from "@/lib/hooks/useResponsive";
 import { useUserSession } from "@/lib/hooks/useUserSession";
 
-type LeaderboardEntry = {
-  userId: string;
-  name: string;
-  student_number: string;
-  score: number;
+type LeaderboardTab = "BALANCE" | "ROI";
+
+type LeaderboardDisplayRow = {
+  key: string;
+  rank: number;
+  username: string;
+  valueText: string;
+};
+
+const LEADERBOARD_TABS: Array<{ id: LeaderboardTab; label: string }> = [
+  { id: "BALANCE", label: "잔고 순위" },
+  { id: "ROI", label: "수익률 순위" },
+];
+
+const valueFormatter = new Intl.NumberFormat("ko-KR", {
+  maximumFractionDigits: 2,
+});
+
+const formatRoiPercent = (value: number) => {
+  const formatted = valueFormatter.format(Math.abs(value));
+  return `${value >= 0 ? "+" : "-"}${formatted}%`;
+};
+
+const getRankTextClass = (rank: number) => {
+  if (rank === 1) {
+    return "text-white";
+  }
+
+  if (rank === 2 || rank === 3) {
+    return "text-tokhin-green";
+  }
+
+  return "text-stone-500";
+};
+
+const getRankBackgroundClass = (rank: number) => {
+  return rank === 1 ? "bg-tokhin-green" : "bg-neutral-50";
 };
 
 export default function LeaderboardPage() {
@@ -17,21 +54,82 @@
   const { session, isLoading: isSessionLoading } = useUserSession({
     requireAuth: true,
   });
-  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
+
+  const [activeTab, setActiveTab] = useState<LeaderboardTab>("BALANCE");
+  const [balanceLeaderboard, setBalanceLeaderboard] = useState<
+    LeaderboardBalanceItem[]
+  >([]);
+  const [roiLeaderboard, setRoiLeaderboard] = useState<LeaderboardRoiItem[]>([]);
   const [isLoading, setIsLoading] = useState(true);
+  const [errorMessage, setErrorMessage] = useState<string | null>(null);
 
   useEffect(() => {
-    if (!session) return;
+    if (!session?.user_id) {
+      return;
+    }
 
+    let isCancelled = false;
     const fetchLeaderboard = async () => {
       setIsLoading(true);
-      const data = await getLeaderboard("2025-10-05", "2025-12-31");
-      setLeaderboard(data);
-      setIsLoading(false);
+      setErrorMessage(null);
+
+      try {
+        const [balanceData, roiData] = await Promise.all([
+          getLeaderboardBalance(),
+          getLeaderboardRoi(),
+        ]);
+
+        if (isCancelled) {
+          return;
+        }
+
+        setBalanceLeaderboard(balanceData);
+        setRoiLeaderboard(roiData);
+      } catch (error) {
+        if (isCancelled) {
+          return;
+        }
+
+        const message =
+          error instanceof Error
+            ? error.message
+            : "리더보드 조회 중 오류가 발생했습니다";
+
+        setErrorMessage(message);
+        setBalanceLeaderboard([]);
+        setRoiLeaderboard([]);
+      } finally {
+        if (!isCancelled) {
+          setIsLoading(false);
+        }
+      }
     };
+
     fetchLeaderboard();
-  }, [session]);
 
+    return () => {
+      isCancelled = true;
+    };
+  }, [session?.user_id]);
+
+  const displayRows = useMemo<LeaderboardDisplayRow[]>(() => {
+    if (activeTab === "BALANCE") {
+      return balanceLeaderboard.map((entry) => ({
+        key: `balance-${entry.userId}`,
+        rank: entry.rank,
+        username: entry.username,
+        valueText: valueFormatter.format(entry.balance),
+      }));
+    }
+
+    return roiLeaderboard.map((entry) => ({
+      key: `roi-${entry.userId}`,
+      rank: entry.rank,
+      username: entry.username,
+      valueText: formatRoiPercent(entry.roiPercent),
+    }));
+  }, [activeTab, balanceLeaderboard, roiLeaderboard]);
+
   if (isSessionLoading) {
     return <p className="py-20 text-center text-zinc-500">로딩 중...</p>;
   }
@@ -47,63 +145,71 @@
           isMobile ? "text-xl" : "text-4xl"
         }`}
       >
-        실시간 순위
+        리더보드
       </h1>
 
+      <div className="mb-5 border-b border-zinc-200">
+        <div className="grid grid-cols-2">
+          {LEADERBOARD_TABS.map((tab) => (
+            <button
+              key={tab.id}
+              type="button"
+              onClick={() => setActiveTab(tab.id)}
+              className={`pb-3 text-sm font-semibold transition-colors ${
+                activeTab === tab.id
+                  ? "border-b-2 border-black text-black"
+                  : "text-zinc-400"
+              }`}
+            >
+              {tab.label}
+            </button>
+          ))}
+        </div>
+      </div>
+
       {isLoading ? (
         <p className="text-center">로딩 중...</p>
-      ) : leaderboard.length === 0 ? (
+      ) : errorMessage ? (
+        <p className="text-center text-red-500">{errorMessage}</p>
+      ) : displayRows.length === 0 ? (
         <p className="text-center text-gray-600">
-          아직 순위표를 볼 수 없습니다.
+          아직 {activeTab === "BALANCE" ? "잔고" : "수익률"} 순위표를 볼 수
+          없습니다.
         </p>
       ) : (
         <div>
           <ul className="divide-y divide-transparent">
-            {leaderboard.map((entry, index) => (
+            {displayRows.map((entry) => (
               <li
-                key={entry.userId}
+                key={entry.key}
                 className={`flex items-center ${
                   isMobile ? "px-4" : "px-4"
-                } h-12 ${
-                  index === 0 ? "bg-tokhin-green" : "bg-neutral-50"
-                } rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
+                } h-12 ${getRankBackgroundClass(
+                  entry.rank
+                )} rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
               >
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${
+                  className={`font-base ${getRankTextClass(entry.rank)} ${
                     isMobile
                       ? "text-base w-6 text-center"
                       : "text-lg w-12 text-center"
                   }`}
                 >
-                  {index + 1}
+                  {entry.rank}
                 </span>
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
+                  className={`font-base ${getRankTextClass(
+                    entry.rank
+                  )} flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
                 >
-                  {entry.name}
+                  {entry.username}
                 </span>
                 <span
-                  className={`font-normal ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${isMobile ? "text-base px-1" : "text-2xl"}`}
+                  className={`font-normal ${getRankTextClass(entry.rank)} ${
+                    isMobile ? "text-base px-1" : "text-2xl"
+                  }`}
                 >
-                  {entry.score}
+                  {entry.valueText}
                 </span>
               </li>
             ))}
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/prd.json b/prd.json
index 88f5357a539191312b41088c3cface270425eb93..79cc576c022136f0b53f7c46f944a6e697bb4e50
--- a/prd.json
+++ b/prd.json
@@ -124,7 +124,7 @@
       "id": "US-008",
       "title": "\ub9ac\ub354\ubcf4\ub4dc \u2014 \ucf54\uc778 \uc794\uace0 + \uc218\uc775\ub960 \ubcf5\ud569 \ub7ad\ud0b9",
       "priority": 8,
-      "passes": false,
+      "passes": true,
       "description": "\ub9ac\ub354\ubcf4\ub4dc\ub97c \ucf54\uc778 \uc794\uace0 \uc21c\uc704\uc640 \uc218\uc775\ub960 \uc21c\uc704 \ub450 \uac00\uc9c0 \ud0ed\uc73c\ub85c \uc81c\uacf5\ud55c\ub2e4. \ub514\uc790\uc778\uc740 prd.md\uc758 W-07 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ucc38\uc870.",
       "acceptanceCriteria": [
         "REQ-053 get_leaderboard_balance() RETURNS TABLE(...) RPC \ud568\uc218. \ucf54\uc778 \uc794\uace0 \uae30\uc900 \ub0b4\ub9bc\ucc28\uc21c \uc815\ub82c. \ubc18\ud658: rank, user_id, username, balance.",
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;

file update:
diff --git a/app/leaderboard/page.tsx b/app/leaderboard/page.tsx
index 2adb2b80e2f8fed380614a6cbd203a475e0a18fa..cf6731cc7b5e350119555e4e2824b14c9a2f3f56
--- a/app/leaderboard/page.tsx
+++ b/app/leaderboard/page.tsx
@@ -1,15 +1,52 @@
 "use client";
 
-import { useState, useEffect } from "react";
-import { getLeaderboard } from "@/lib/api";
+import { useEffect, useMemo, useState } from "react";
+import {
+  getLeaderboardBalance,
+  getLeaderboardRoi,
+  type LeaderboardBalanceItem,
+  type LeaderboardRoiItem,
+} from "@/lib/api";
 import { useIsMobile } from "@/lib/hooks/useResponsive";
 import { useUserSession } from "@/lib/hooks/useUserSession";
 
-type LeaderboardEntry = {
-  userId: string;
-  name: string;
-  student_number: string;
-  score: number;
+type LeaderboardTab = "BALANCE" | "ROI";
+
+type LeaderboardDisplayRow = {
+  key: string;
+  rank: number;
+  username: string;
+  valueText: string;
+};
+
+const LEADERBOARD_TABS: Array<{ id: LeaderboardTab; label: string }> = [
+  { id: "BALANCE", label: "잔고 순위" },
+  { id: "ROI", label: "수익률 순위" },
+];
+
+const valueFormatter = new Intl.NumberFormat("ko-KR", {
+  maximumFractionDigits: 2,
+});
+
+const formatRoiPercent = (value: number) => {
+  const formatted = valueFormatter.format(Math.abs(value));
+  return `${value >= 0 ? "+" : "-"}${formatted}%`;
+};
+
+const getRankTextClass = (rank: number) => {
+  if (rank === 1) {
+    return "text-white";
+  }
+
+  if (rank === 2 || rank === 3) {
+    return "text-tokhin-green";
+  }
+
+  return "text-stone-500";
+};
+
+const getRankBackgroundClass = (rank: number) => {
+  return rank === 1 ? "bg-tokhin-green" : "bg-neutral-50";
 };
 
 export default function LeaderboardPage() {
@@ -17,21 +54,82 @@
   const { session, isLoading: isSessionLoading } = useUserSession({
     requireAuth: true,
   });
-  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
+
+  const [activeTab, setActiveTab] = useState<LeaderboardTab>("BALANCE");
+  const [balanceLeaderboard, setBalanceLeaderboard] = useState<
+    LeaderboardBalanceItem[]
+  >([]);
+  const [roiLeaderboard, setRoiLeaderboard] = useState<LeaderboardRoiItem[]>([]);
   const [isLoading, setIsLoading] = useState(true);
+  const [errorMessage, setErrorMessage] = useState<string | null>(null);
 
   useEffect(() => {
-    if (!session) return;
+    if (!session?.user_id) {
+      return;
+    }
 
+    let isCancelled = false;
     const fetchLeaderboard = async () => {
       setIsLoading(true);
-      const data = await getLeaderboard("2025-10-05", "2025-12-31");
-      setLeaderboard(data);
-      setIsLoading(false);
+      setErrorMessage(null);
+
+      try {
+        const [balanceData, roiData] = await Promise.all([
+          getLeaderboardBalance(),
+          getLeaderboardRoi(),
+        ]);
+
+        if (isCancelled) {
+          return;
+        }
+
+        setBalanceLeaderboard(balanceData);
+        setRoiLeaderboard(roiData);
+      } catch (error) {
+        if (isCancelled) {
+          return;
+        }
+
+        const message =
+          error instanceof Error
+            ? error.message
+            : "리더보드 조회 중 오류가 발생했습니다";
+
+        setErrorMessage(message);
+        setBalanceLeaderboard([]);
+        setRoiLeaderboard([]);
+      } finally {
+        if (!isCancelled) {
+          setIsLoading(false);
+        }
+      }
     };
+
     fetchLeaderboard();
-  }, [session]);
 
+    return () => {
+      isCancelled = true;
+    };
+  }, [session?.user_id]);
+
+  const displayRows = useMemo<LeaderboardDisplayRow[]>(() => {
+    if (activeTab === "BALANCE") {
+      return balanceLeaderboard.map((entry) => ({
+        key: `balance-${entry.userId}`,
+        rank: entry.rank,
+        username: entry.username,
+        valueText: valueFormatter.format(entry.balance),
+      }));
+    }
+
+    return roiLeaderboard.map((entry) => ({
+      key: `roi-${entry.userId}`,
+      rank: entry.rank,
+      username: entry.username,
+      valueText: formatRoiPercent(entry.roiPercent),
+    }));
+  }, [activeTab, balanceLeaderboard, roiLeaderboard]);
+
   if (isSessionLoading) {
     return <p className="py-20 text-center text-zinc-500">로딩 중...</p>;
   }
@@ -47,63 +145,71 @@
           isMobile ? "text-xl" : "text-4xl"
         }`}
       >
-        실시간 순위
+        리더보드
       </h1>
 
+      <div className="mb-5 border-b border-zinc-200">
+        <div className="grid grid-cols-2">
+          {LEADERBOARD_TABS.map((tab) => (
+            <button
+              key={tab.id}
+              type="button"
+              onClick={() => setActiveTab(tab.id)}
+              className={`pb-3 text-sm font-semibold transition-colors ${
+                activeTab === tab.id
+                  ? "border-b-2 border-black text-black"
+                  : "text-zinc-400"
+              }`}
+            >
+              {tab.label}
+            </button>
+          ))}
+        </div>
+      </div>
+
       {isLoading ? (
         <p className="text-center">로딩 중...</p>
-      ) : leaderboard.length === 0 ? (
+      ) : errorMessage ? (
+        <p className="text-center text-red-500">{errorMessage}</p>
+      ) : displayRows.length === 0 ? (
         <p className="text-center text-gray-600">
-          아직 순위표를 볼 수 없습니다.
+          아직 {activeTab === "BALANCE" ? "잔고" : "수익률"} 순위표를 볼 수
+          없습니다.
         </p>
       ) : (
         <div>
           <ul className="divide-y divide-transparent">
-            {leaderboard.map((entry, index) => (
+            {displayRows.map((entry) => (
               <li
-                key={entry.userId}
+                key={entry.key}
                 className={`flex items-center ${
                   isMobile ? "px-4" : "px-4"
-                } h-12 ${
-                  index === 0 ? "bg-tokhin-green" : "bg-neutral-50"
-                } rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
+                } h-12 ${getRankBackgroundClass(
+                  entry.rank
+                )} rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
               >
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${
+                  className={`font-base ${getRankTextClass(entry.rank)} ${
                     isMobile
                       ? "text-base w-6 text-center"
                       : "text-lg w-12 text-center"
                   }`}
                 >
-                  {index + 1}
+                  {entry.rank}
                 </span>
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
+                  className={`font-base ${getRankTextClass(
+                    entry.rank
+                  )} flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
                 >
-                  {entry.name}
+                  {entry.username}
                 </span>
                 <span
-                  className={`font-normal ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${isMobile ? "text-base px-1" : "text-2xl"}`}
+                  className={`font-normal ${getRankTextClass(entry.rank)} ${
+                    isMobile ? "text-base px-1" : "text-2xl"
+                  }`}
                 >
-                  {entry.score}
+                  {entry.valueText}
                 </span>
               </li>
             ))}
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/prd.json b/prd.json
index 88f5357a539191312b41088c3cface270425eb93..79cc576c022136f0b53f7c46f944a6e697bb4e50
--- a/prd.json
+++ b/prd.json
@@ -124,7 +124,7 @@
       "id": "US-008",
       "title": "\ub9ac\ub354\ubcf4\ub4dc \u2014 \ucf54\uc778 \uc794\uace0 + \uc218\uc775\ub960 \ubcf5\ud569 \ub7ad\ud0b9",
       "priority": 8,
-      "passes": false,
+      "passes": true,
       "description": "\ub9ac\ub354\ubcf4\ub4dc\ub97c \ucf54\uc778 \uc794\uace0 \uc21c\uc704\uc640 \uc218\uc775\ub960 \uc21c\uc704 \ub450 \uac00\uc9c0 \ud0ed\uc73c\ub85c \uc81c\uacf5\ud55c\ub2e4. \ub514\uc790\uc778\uc740 prd.md\uc758 W-07 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ucc38\uc870.",
       "acceptanceCriteria": [
         "REQ-053 get_leaderboard_balance() RETURNS TABLE(...) RPC \ud568\uc218. \ucf54\uc778 \uc794\uace0 \uae30\uc900 \ub0b4\ub9bc\ucc28\uc21c \uc815\ub82c. \ubc18\ud658: rank, user_id, username, balance.",
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Updating plan step statuses**
Plan update
  ✓ Inspect current leaderboard data flow and identify exact files/migrations to update for US-008.
  ✓ Add Supabase migration for REQ-053/REQ-054 RPC functions and grants.
  ✓ Update lib/api.ts with new leaderboard RPC client functions and types.
  ✓ Refactor app/leaderboard/page.tsx to support balance/ROI tab switching with required ranking styles.
  → Run verification (lint/build subset) and update prd.json story status for US-008 if all criteria are met.
file update:
diff --git a/app/leaderboard/page.tsx b/app/leaderboard/page.tsx
index 2adb2b80e2f8fed380614a6cbd203a475e0a18fa..cf6731cc7b5e350119555e4e2824b14c9a2f3f56
--- a/app/leaderboard/page.tsx
+++ b/app/leaderboard/page.tsx
@@ -1,15 +1,52 @@
 "use client";
 
-import { useState, useEffect } from "react";
-import { getLeaderboard } from "@/lib/api";
+import { useEffect, useMemo, useState } from "react";
+import {
+  getLeaderboardBalance,
+  getLeaderboardRoi,
+  type LeaderboardBalanceItem,
+  type LeaderboardRoiItem,
+} from "@/lib/api";
 import { useIsMobile } from "@/lib/hooks/useResponsive";
 import { useUserSession } from "@/lib/hooks/useUserSession";
 
-type LeaderboardEntry = {
-  userId: string;
-  name: string;
-  student_number: string;
-  score: number;
+type LeaderboardTab = "BALANCE" | "ROI";
+
+type LeaderboardDisplayRow = {
+  key: string;
+  rank: number;
+  username: string;
+  valueText: string;
+};
+
+const LEADERBOARD_TABS: Array<{ id: LeaderboardTab; label: string }> = [
+  { id: "BALANCE", label: "잔고 순위" },
+  { id: "ROI", label: "수익률 순위" },
+];
+
+const valueFormatter = new Intl.NumberFormat("ko-KR", {
+  maximumFractionDigits: 2,
+});
+
+const formatRoiPercent = (value: number) => {
+  const formatted = valueFormatter.format(Math.abs(value));
+  return `${value >= 0 ? "+" : "-"}${formatted}%`;
+};
+
+const getRankTextClass = (rank: number) => {
+  if (rank === 1) {
+    return "text-white";
+  }
+
+  if (rank === 2 || rank === 3) {
+    return "text-tokhin-green";
+  }
+
+  return "text-stone-500";
+};
+
+const getRankBackgroundClass = (rank: number) => {
+  return rank === 1 ? "bg-tokhin-green" : "bg-neutral-50";
 };
 
 export default function LeaderboardPage() {
@@ -17,21 +54,82 @@
   const { session, isLoading: isSessionLoading } = useUserSession({
     requireAuth: true,
   });
-  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
+
+  const [activeTab, setActiveTab] = useState<LeaderboardTab>("BALANCE");
+  const [balanceLeaderboard, setBalanceLeaderboard] = useState<
+    LeaderboardBalanceItem[]
+  >([]);
+  const [roiLeaderboard, setRoiLeaderboard] = useState<LeaderboardRoiItem[]>([]);
   const [isLoading, setIsLoading] = useState(true);
+  const [errorMessage, setErrorMessage] = useState<string | null>(null);
 
   useEffect(() => {
-    if (!session) return;
+    if (!session?.user_id) {
+      return;
+    }
 
+    let isCancelled = false;
     const fetchLeaderboard = async () => {
       setIsLoading(true);
-      const data = await getLeaderboard("2025-10-05", "2025-12-31");
-      setLeaderboard(data);
-      setIsLoading(false);
+      setErrorMessage(null);
+
+      try {
+        const [balanceData, roiData] = await Promise.all([
+          getLeaderboardBalance(),
+          getLeaderboardRoi(),
+        ]);
+
+        if (isCancelled) {
+          return;
+        }
+
+        setBalanceLeaderboard(balanceData);
+        setRoiLeaderboard(roiData);
+      } catch (error) {
+        if (isCancelled) {
+          return;
+        }
+
+        const message =
+          error instanceof Error
+            ? error.message
+            : "리더보드 조회 중 오류가 발생했습니다";
+
+        setErrorMessage(message);
+        setBalanceLeaderboard([]);
+        setRoiLeaderboard([]);
+      } finally {
+        if (!isCancelled) {
+          setIsLoading(false);
+        }
+      }
     };
+
     fetchLeaderboard();
-  }, [session]);
 
+    return () => {
+      isCancelled = true;
+    };
+  }, [session?.user_id]);
+
+  const displayRows = useMemo<LeaderboardDisplayRow[]>(() => {
+    if (activeTab === "BALANCE") {
+      return balanceLeaderboard.map((entry) => ({
+        key: `balance-${entry.userId}`,
+        rank: entry.rank,
+        username: entry.username,
+        valueText: valueFormatter.format(entry.balance),
+      }));
+    }
+
+    return roiLeaderboard.map((entry) => ({
+      key: `roi-${entry.userId}`,
+      rank: entry.rank,
+      username: entry.username,
+      valueText: formatRoiPercent(entry.roiPercent),
+    }));
+  }, [activeTab, balanceLeaderboard, roiLeaderboard]);
+
   if (isSessionLoading) {
     return <p className="py-20 text-center text-zinc-500">로딩 중...</p>;
   }
@@ -47,63 +145,71 @@
           isMobile ? "text-xl" : "text-4xl"
         }`}
       >
-        실시간 순위
+        리더보드
       </h1>
 
+      <div className="mb-5 border-b border-zinc-200">
+        <div className="grid grid-cols-2">
+          {LEADERBOARD_TABS.map((tab) => (
+            <button
+              key={tab.id}
+              type="button"
+              onClick={() => setActiveTab(tab.id)}
+              className={`pb-3 text-sm font-semibold transition-colors ${
+                activeTab === tab.id
+                  ? "border-b-2 border-black text-black"
+                  : "text-zinc-400"
+              }`}
+            >
+              {tab.label}
+            </button>
+          ))}
+        </div>
+      </div>
+
       {isLoading ? (
         <p className="text-center">로딩 중...</p>
-      ) : leaderboard.length === 0 ? (
+      ) : errorMessage ? (
+        <p className="text-center text-red-500">{errorMessage}</p>
+      ) : displayRows.length === 0 ? (
         <p className="text-center text-gray-600">
-          아직 순위표를 볼 수 없습니다.
+          아직 {activeTab === "BALANCE" ? "잔고" : "수익률"} 순위표를 볼 수
+          없습니다.
         </p>
       ) : (
         <div>
           <ul className="divide-y divide-transparent">
-            {leaderboard.map((entry, index) => (
+            {displayRows.map((entry) => (
               <li
-                key={entry.userId}
+                key={entry.key}
                 className={`flex items-center ${
                   isMobile ? "px-4" : "px-4"
-                } h-12 ${
-                  index === 0 ? "bg-tokhin-green" : "bg-neutral-50"
-                } rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
+                } h-12 ${getRankBackgroundClass(
+                  entry.rank
+                )} rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
               >
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${
+                  className={`font-base ${getRankTextClass(entry.rank)} ${
                     isMobile
                       ? "text-base w-6 text-center"
                       : "text-lg w-12 text-center"
                   }`}
                 >
-                  {index + 1}
+                  {entry.rank}
                 </span>
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
+                  className={`font-base ${getRankTextClass(
+                    entry.rank
+                  )} flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
                 >
-                  {entry.name}
+                  {entry.username}
                 </span>
                 <span
-                  className={`font-normal ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${isMobile ? "text-base px-1" : "text-2xl"}`}
+                  className={`font-normal ${getRankTextClass(entry.rank)} ${
+                    isMobile ? "text-base px-1" : "text-2xl"
+                  }`}
                 >
-                  {entry.score}
+                  {entry.valueText}
                 </span>
               </li>
             ))}
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/prd.json b/prd.json
index 88f5357a539191312b41088c3cface270425eb93..79cc576c022136f0b53f7c46f944a6e697bb4e50
--- a/prd.json
+++ b/prd.json
@@ -124,7 +124,7 @@
       "id": "US-008",
       "title": "\ub9ac\ub354\ubcf4\ub4dc \u2014 \ucf54\uc778 \uc794\uace0 + \uc218\uc775\ub960 \ubcf5\ud569 \ub7ad\ud0b9",
       "priority": 8,
-      "passes": false,
+      "passes": true,
       "description": "\ub9ac\ub354\ubcf4\ub4dc\ub97c \ucf54\uc778 \uc794\uace0 \uc21c\uc704\uc640 \uc218\uc775\ub960 \uc21c\uc704 \ub450 \uac00\uc9c0 \ud0ed\uc73c\ub85c \uc81c\uacf5\ud55c\ub2e4. \ub514\uc790\uc778\uc740 prd.md\uc758 W-07 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ucc38\uc870.",
       "acceptanceCriteria": [
         "REQ-053 get_leaderboard_balance() RETURNS TABLE(...) RPC \ud568\uc218. \ucf54\uc778 \uc794\uace0 \uae30\uc900 \ub0b4\ub9bc\ucc28\uc21c \uc815\ub82c. \ubc18\ud658: rank, user_id, username, balance.",
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Running lint to check formatting**
exec
/bin/zsh -lc 'npm run lint' in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 1.38s:

> lint
> next lint

✔ No ESLint warnings or errors
file update:
diff --git a/app/leaderboard/page.tsx b/app/leaderboard/page.tsx
index 2adb2b80e2f8fed380614a6cbd203a475e0a18fa..cf6731cc7b5e350119555e4e2824b14c9a2f3f56
--- a/app/leaderboard/page.tsx
+++ b/app/leaderboard/page.tsx
@@ -1,15 +1,52 @@
 "use client";
 
-import { useState, useEffect } from "react";
-import { getLeaderboard } from "@/lib/api";
+import { useEffect, useMemo, useState } from "react";
+import {
+  getLeaderboardBalance,
+  getLeaderboardRoi,
+  type LeaderboardBalanceItem,
+  type LeaderboardRoiItem,
+} from "@/lib/api";
 import { useIsMobile } from "@/lib/hooks/useResponsive";
 import { useUserSession } from "@/lib/hooks/useUserSession";
 
-type LeaderboardEntry = {
-  userId: string;
-  name: string;
-  student_number: string;
-  score: number;
+type LeaderboardTab = "BALANCE" | "ROI";
+
+type LeaderboardDisplayRow = {
+  key: string;
+  rank: number;
+  username: string;
+  valueText: string;
+};
+
+const LEADERBOARD_TABS: Array<{ id: LeaderboardTab; label: string }> = [
+  { id: "BALANCE", label: "잔고 순위" },
+  { id: "ROI", label: "수익률 순위" },
+];
+
+const valueFormatter = new Intl.NumberFormat("ko-KR", {
+  maximumFractionDigits: 2,
+});
+
+const formatRoiPercent = (value: number) => {
+  const formatted = valueFormatter.format(Math.abs(value));
+  return `${value >= 0 ? "+" : "-"}${formatted}%`;
+};
+
+const getRankTextClass = (rank: number) => {
+  if (rank === 1) {
+    return "text-white";
+  }
+
+  if (rank === 2 || rank === 3) {
+    return "text-tokhin-green";
+  }
+
+  return "text-stone-500";
+};
+
+const getRankBackgroundClass = (rank: number) => {
+  return rank === 1 ? "bg-tokhin-green" : "bg-neutral-50";
 };
 
 export default function LeaderboardPage() {
@@ -17,21 +54,82 @@
   const { session, isLoading: isSessionLoading } = useUserSession({
     requireAuth: true,
   });
-  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
+
+  const [activeTab, setActiveTab] = useState<LeaderboardTab>("BALANCE");
+  const [balanceLeaderboard, setBalanceLeaderboard] = useState<
+    LeaderboardBalanceItem[]
+  >([]);
+  const [roiLeaderboard, setRoiLeaderboard] = useState<LeaderboardRoiItem[]>([]);
   const [isLoading, setIsLoading] = useState(true);
+  const [errorMessage, setErrorMessage] = useState<string | null>(null);
 
   useEffect(() => {
-    if (!session) return;
+    if (!session?.user_id) {
+      return;
+    }
 
+    let isCancelled = false;
     const fetchLeaderboard = async () => {
       setIsLoading(true);
-      const data = await getLeaderboard("2025-10-05", "2025-12-31");
-      setLeaderboard(data);
-      setIsLoading(false);
+      setErrorMessage(null);
+
+      try {
+        const [balanceData, roiData] = await Promise.all([
+          getLeaderboardBalance(),
+          getLeaderboardRoi(),
+        ]);
+
+        if (isCancelled) {
+          return;
+        }
+
+        setBalanceLeaderboard(balanceData);
+        setRoiLeaderboard(roiData);
+      } catch (error) {
+        if (isCancelled) {
+          return;
+        }
+
+        const message =
+          error instanceof Error
+            ? error.message
+            : "리더보드 조회 중 오류가 발생했습니다";
+
+        setErrorMessage(message);
+        setBalanceLeaderboard([]);
+        setRoiLeaderboard([]);
+      } finally {
+        if (!isCancelled) {
+          setIsLoading(false);
+        }
+      }
     };
+
     fetchLeaderboard();
-  }, [session]);
 
+    return () => {
+      isCancelled = true;
+    };
+  }, [session?.user_id]);
+
+  const displayRows = useMemo<LeaderboardDisplayRow[]>(() => {
+    if (activeTab === "BALANCE") {
+      return balanceLeaderboard.map((entry) => ({
+        key: `balance-${entry.userId}`,
+        rank: entry.rank,
+        username: entry.username,
+        valueText: valueFormatter.format(entry.balance),
+      }));
+    }
+
+    return roiLeaderboard.map((entry) => ({
+      key: `roi-${entry.userId}`,
+      rank: entry.rank,
+      username: entry.username,
+      valueText: formatRoiPercent(entry.roiPercent),
+    }));
+  }, [activeTab, balanceLeaderboard, roiLeaderboard]);
+
   if (isSessionLoading) {
     return <p className="py-20 text-center text-zinc-500">로딩 중...</p>;
   }
@@ -47,63 +145,71 @@
           isMobile ? "text-xl" : "text-4xl"
         }`}
       >
-        실시간 순위
+        리더보드
       </h1>
 
+      <div className="mb-5 border-b border-zinc-200">
+        <div className="grid grid-cols-2">
+          {LEADERBOARD_TABS.map((tab) => (
+            <button
+              key={tab.id}
+              type="button"
+              onClick={() => setActiveTab(tab.id)}
+              className={`pb-3 text-sm font-semibold transition-colors ${
+                activeTab === tab.id
+                  ? "border-b-2 border-black text-black"
+                  : "text-zinc-400"
+              }`}
+            >
+              {tab.label}
+            </button>
+          ))}
+        </div>
+      </div>
+
       {isLoading ? (
         <p className="text-center">로딩 중...</p>
-      ) : leaderboard.length === 0 ? (
+      ) : errorMessage ? (
+        <p className="text-center text-red-500">{errorMessage}</p>
+      ) : displayRows.length === 0 ? (
         <p className="text-center text-gray-600">
-          아직 순위표를 볼 수 없습니다.
+          아직 {activeTab === "BALANCE" ? "잔고" : "수익률"} 순위표를 볼 수
+          없습니다.
         </p>
       ) : (
         <div>
           <ul className="divide-y divide-transparent">
-            {leaderboard.map((entry, index) => (
+            {displayRows.map((entry) => (
               <li
-                key={entry.userId}
+                key={entry.key}
                 className={`flex items-center ${
                   isMobile ? "px-4" : "px-4"
-                } h-12 ${
-                  index === 0 ? "bg-tokhin-green" : "bg-neutral-50"
-                } rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
+                } h-12 ${getRankBackgroundClass(
+                  entry.rank
+                )} rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
               >
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${
+                  className={`font-base ${getRankTextClass(entry.rank)} ${
                     isMobile
                       ? "text-base w-6 text-center"
                       : "text-lg w-12 text-center"
                   }`}
                 >
-                  {index + 1}
+                  {entry.rank}
                 </span>
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
+                  className={`font-base ${getRankTextClass(
+                    entry.rank
+                  )} flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
                 >
-                  {entry.name}
+                  {entry.username}
                 </span>
                 <span
-                  className={`font-normal ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${isMobile ? "text-base px-1" : "text-2xl"}`}
+                  className={`font-normal ${getRankTextClass(entry.rank)} ${
+                    isMobile ? "text-base px-1" : "text-2xl"
+                  }`}
                 >
-                  {entry.score}
+                  {entry.valueText}
                 </span>
               </li>
             ))}
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/prd.json b/prd.json
index 88f5357a539191312b41088c3cface270425eb93..79cc576c022136f0b53f7c46f944a6e697bb4e50
--- a/prd.json
+++ b/prd.json
@@ -124,7 +124,7 @@
       "id": "US-008",
       "title": "\ub9ac\ub354\ubcf4\ub4dc \u2014 \ucf54\uc778 \uc794\uace0 + \uc218\uc775\ub960 \ubcf5\ud569 \ub7ad\ud0b9",
       "priority": 8,
-      "passes": false,
+      "passes": true,
       "description": "\ub9ac\ub354\ubcf4\ub4dc\ub97c \ucf54\uc778 \uc794\uace0 \uc21c\uc704\uc640 \uc218\uc775\ub960 \uc21c\uc704 \ub450 \uac00\uc9c0 \ud0ed\uc73c\ub85c \uc81c\uacf5\ud55c\ub2e4. \ub514\uc790\uc778\uc740 prd.md\uc758 W-07 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ucc38\uc870.",
       "acceptanceCriteria": [
         "REQ-053 get_leaderboard_balance() RETURNS TABLE(...) RPC \ud568\uc218. \ucf54\uc778 \uc794\uace0 \uae30\uc900 \ub0b4\ub9bc\ucc28\uc21c \uc815\ub82c. \ubc18\ud658: rank, user_id, username, balance.",
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Running build to verify typecheck**
exec
/bin/zsh -lc 'npm run build' in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 9.23s:

> build
> next build

   ▲ Next.js 15.3.5
   - Environments: .env.local

   Creating an optimized production build ...
Browserslist: browsers data (caniuse-lite) is 8 months old. Please run:
  npx update-browserslist-db@latest
  Why you should do it regularly: https://github.com/browserslist/update-db#readme
 ✓ Compiled successfully in 0ms
   Linting and checking validity of types ...
   Collecting page data ...
   Generating static pages (0/12) ...
   Generating static pages (3/12) 
   Generating static pages (6/12) 
   Generating static pages (9/12) 
 ✓ Generating static pages (12/12)
   Finalizing page optimization ...
file update:
diff --git a/app/leaderboard/page.tsx b/app/leaderboard/page.tsx
index 2adb2b80e2f8fed380614a6cbd203a475e0a18fa..cf6731cc7b5e350119555e4e2824b14c9a2f3f56
--- a/app/leaderboard/page.tsx
+++ b/app/leaderboard/page.tsx
@@ -1,15 +1,52 @@
 "use client";
 
-import { useState, useEffect } from "react";
-import { getLeaderboard } from "@/lib/api";
+import { useEffect, useMemo, useState } from "react";
+import {
+  getLeaderboardBalance,
+  getLeaderboardRoi,
+  type LeaderboardBalanceItem,
+  type LeaderboardRoiItem,
+} from "@/lib/api";
 import { useIsMobile } from "@/lib/hooks/useResponsive";
 import { useUserSession } from "@/lib/hooks/useUserSession";
 
-type LeaderboardEntry = {
-  userId: string;
-  name: string;
-  student_number: string;
-  score: number;
+type LeaderboardTab = "BALANCE" | "ROI";
+
+type LeaderboardDisplayRow = {
+  key: string;
+  rank: number;
+  username: string;
+  valueText: string;
+};
+
+const LEADERBOARD_TABS: Array<{ id: LeaderboardTab; label: string }> = [
+  { id: "BALANCE", label: "잔고 순위" },
+  { id: "ROI", label: "수익률 순위" },
+];
+
+const valueFormatter = new Intl.NumberFormat("ko-KR", {
+  maximumFractionDigits: 2,
+});
+
+const formatRoiPercent = (value: number) => {
+  const formatted = valueFormatter.format(Math.abs(value));
+  return `${value >= 0 ? "+" : "-"}${formatted}%`;
+};
+
+const getRankTextClass = (rank: number) => {
+  if (rank === 1) {
+    return "text-white";
+  }
+
+  if (rank === 2 || rank === 3) {
+    return "text-tokhin-green";
+  }
+
+  return "text-stone-500";
+};
+
+const getRankBackgroundClass = (rank: number) => {
+  return rank === 1 ? "bg-tokhin-green" : "bg-neutral-50";
 };
 
 export default function LeaderboardPage() {
@@ -17,21 +54,82 @@
   const { session, isLoading: isSessionLoading } = useUserSession({
     requireAuth: true,
   });
-  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
+
+  const [activeTab, setActiveTab] = useState<LeaderboardTab>("BALANCE");
+  const [balanceLeaderboard, setBalanceLeaderboard] = useState<
+    LeaderboardBalanceItem[]
+  >([]);
+  const [roiLeaderboard, setRoiLeaderboard] = useState<LeaderboardRoiItem[]>([]);
   const [isLoading, setIsLoading] = useState(true);
+  const [errorMessage, setErrorMessage] = useState<string | null>(null);
 
   useEffect(() => {
-    if (!session) return;
+    if (!session?.user_id) {
+      return;
+    }
 
+    let isCancelled = false;
     const fetchLeaderboard = async () => {
       setIsLoading(true);
-      const data = await getLeaderboard("2025-10-05", "2025-12-31");
-      setLeaderboard(data);
-      setIsLoading(false);
+      setErrorMessage(null);
+
+      try {
+        const [balanceData, roiData] = await Promise.all([
+          getLeaderboardBalance(),
+          getLeaderboardRoi(),
+        ]);
+
+        if (isCancelled) {
+          return;
+        }
+
+        setBalanceLeaderboard(balanceData);
+        setRoiLeaderboard(roiData);
+      } catch (error) {
+        if (isCancelled) {
+          return;
+        }
+
+        const message =
+          error instanceof Error
+            ? error.message
+            : "리더보드 조회 중 오류가 발생했습니다";
+
+        setErrorMessage(message);
+        setBalanceLeaderboard([]);
+        setRoiLeaderboard([]);
+      } finally {
+        if (!isCancelled) {
+          setIsLoading(false);
+        }
+      }
     };
+
     fetchLeaderboard();
-  }, [session]);
 
+    return () => {
+      isCancelled = true;
+    };
+  }, [session?.user_id]);
+
+  const displayRows = useMemo<LeaderboardDisplayRow[]>(() => {
+    if (activeTab === "BALANCE") {
+      return balanceLeaderboard.map((entry) => ({
+        key: `balance-${entry.userId}`,
+        rank: entry.rank,
+        username: entry.username,
+        valueText: valueFormatter.format(entry.balance),
+      }));
+    }
+
+    return roiLeaderboard.map((entry) => ({
+      key: `roi-${entry.userId}`,
+      rank: entry.rank,
+      username: entry.username,
+      valueText: formatRoiPercent(entry.roiPercent),
+    }));
+  }, [activeTab, balanceLeaderboard, roiLeaderboard]);
+
   if (isSessionLoading) {
     return <p className="py-20 text-center text-zinc-500">로딩 중...</p>;
   }
@@ -47,63 +145,71 @@
           isMobile ? "text-xl" : "text-4xl"
         }`}
       >
-        실시간 순위
+        리더보드
       </h1>
 
+      <div className="mb-5 border-b border-zinc-200">
+        <div className="grid grid-cols-2">
+          {LEADERBOARD_TABS.map((tab) => (
+            <button
+              key={tab.id}
+              type="button"
+              onClick={() => setActiveTab(tab.id)}
+              className={`pb-3 text-sm font-semibold transition-colors ${
+                activeTab === tab.id
+                  ? "border-b-2 border-black text-black"
+                  : "text-zinc-400"
+              }`}
+            >
+              {tab.label}
+            </button>
+          ))}
+        </div>
+      </div>
+
       {isLoading ? (
         <p className="text-center">로딩 중...</p>
-      ) : leaderboard.length === 0 ? (
+      ) : errorMessage ? (
+        <p className="text-center text-red-500">{errorMessage}</p>
+      ) : displayRows.length === 0 ? (
         <p className="text-center text-gray-600">
-          아직 순위표를 볼 수 없습니다.
+          아직 {activeTab === "BALANCE" ? "잔고" : "수익률"} 순위표를 볼 수
+          없습니다.
         </p>
       ) : (
         <div>
           <ul className="divide-y divide-transparent">
-            {leaderboard.map((entry, index) => (
+            {displayRows.map((entry) => (
               <li
-                key={entry.userId}
+                key={entry.key}
                 className={`flex items-center ${
                   isMobile ? "px-4" : "px-4"
-                } h-12 ${
-                  index === 0 ? "bg-tokhin-green" : "bg-neutral-50"
-                } rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
+                } h-12 ${getRankBackgroundClass(
+                  entry.rank
+                )} rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
               >
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${
+                  className={`font-base ${getRankTextClass(entry.rank)} ${
                     isMobile
                       ? "text-base w-6 text-center"
                       : "text-lg w-12 text-center"
                   }`}
                 >
-                  {index + 1}
+                  {entry.rank}
                 </span>
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
+                  className={`font-base ${getRankTextClass(
+                    entry.rank
+                  )} flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
                 >
-                  {entry.name}
+                  {entry.username}
                 </span>
                 <span
-                  className={`font-normal ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${isMobile ? "text-base px-1" : "text-2xl"}`}
+                  className={`font-normal ${getRankTextClass(entry.rank)} ${
+                    isMobile ? "text-base px-1" : "text-2xl"
+                  }`}
                 >
-                  {entry.score}
+                  {entry.valueText}
                 </span>
               </li>
             ))}
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/prd.json b/prd.json
index 88f5357a539191312b41088c3cface270425eb93..79cc576c022136f0b53f7c46f944a6e697bb4e50
--- a/prd.json
+++ b/prd.json
@@ -124,7 +124,7 @@
       "id": "US-008",
       "title": "\ub9ac\ub354\ubcf4\ub4dc \u2014 \ucf54\uc778 \uc794\uace0 + \uc218\uc775\ub960 \ubcf5\ud569 \ub7ad\ud0b9",
       "priority": 8,
-      "passes": false,
+      "passes": true,
       "description": "\ub9ac\ub354\ubcf4\ub4dc\ub97c \ucf54\uc778 \uc794\uace0 \uc21c\uc704\uc640 \uc218\uc775\ub960 \uc21c\uc704 \ub450 \uac00\uc9c0 \ud0ed\uc73c\ub85c \uc81c\uacf5\ud55c\ub2e4. \ub514\uc790\uc778\uc740 prd.md\uc758 W-07 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ucc38\uc870.",
       "acceptanceCriteria": [
         "REQ-053 get_leaderboard_balance() RETURNS TABLE(...) RPC \ud568\uc218. \ucf54\uc778 \uc794\uace0 \uae30\uc900 \ub0b4\ub9bc\ucc28\uc21c \uc815\ub82c. \ubc18\ud658: rank, user_id, username, balance.",
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Verifying local changes**
exec
/bin/zsh -lc 'git status --short' in /Users/jeffrey/WebstormProjects/lookin/lookinexec
/bin/zsh -lc 'git diff -- supabase/migrations/20260224_us008_leaderboard_rankings.sql lib/api.ts app/leaderboard/page.tsx prd.json' in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
 M app/leaderboard/page.tsx
 M lib/api.ts
 M prd.json
?? .ralph/
?? PROGRESS.md
?? supabase/.temp/
?? supabase/migrations/20260224_us008_leaderboard_rankings.sql
 succeeded in 52ms:
diff --git a/app/leaderboard/page.tsx b/app/leaderboard/page.tsx
index 2adb2b8..cf6731c 100644
--- a/app/leaderboard/page.tsx
+++ b/app/leaderboard/page.tsx
@@ -1,15 +1,52 @@
 "use client";
 
-import { useState, useEffect } from "react";
-import { getLeaderboard } from "@/lib/api";
+import { useEffect, useMemo, useState } from "react";
+import {
+  getLeaderboardBalance,
+  getLeaderboardRoi,
+  type LeaderboardBalanceItem,
+  type LeaderboardRoiItem,
+} from "@/lib/api";
 import { useIsMobile } from "@/lib/hooks/useResponsive";
 import { useUserSession } from "@/lib/hooks/useUserSession";
 
-type LeaderboardEntry = {
file update:
diff --git a/app/leaderboard/page.tsx b/app/leaderboard/page.tsx
index 2adb2b80e2f8fed380614a6cbd203a475e0a18fa..cf6731cc7b5e350119555e4e2824b14c9a2f3f56
--- a/app/leaderboard/page.tsx
+++ b/app/leaderboard/page.tsx
@@ -1,15 +1,52 @@
 "use client";
 
-import { useState, useEffect } from "react";
-import { getLeaderboard } from "@/lib/api";
+import { useEffect, useMemo, useState } from "react";
+import {
+  getLeaderboardBalance,
+  getLeaderboardRoi,
+  type LeaderboardBalanceItem,
+  type LeaderboardRoiItem,
+} from "@/lib/api";
 import { useIsMobile } from "@/lib/hooks/useResponsive";
 import { useUserSession } from "@/lib/hooks/useUserSession";
 
-type LeaderboardEntry = {
-  userId: string;
-  name: string;
-  student_number: string;
-  score: number;
+type LeaderboardTab = "BALANCE" | "ROI";
+
+type LeaderboardDisplayRow = {
+  key: string;
+  rank: number;
+  username: string;
+  valueText: string;
+};
+
+const LEADERBOARD_TABS: Array<{ id: LeaderboardTab; label: string }> = [
+  { id: "BALANCE", label: "잔고 순위" },
+  { id: "ROI", label: "수익률 순위" },
+];
+
+const valueFormatter = new Intl.NumberFormat("ko-KR", {
+  maximumFractionDigits: 2,
+});
+
+const formatRoiPercent = (value: number) => {
+  const formatted = valueFormatter.format(Math.abs(value));
+  return `${value >= 0 ? "+" : "-"}${formatted}%`;
+};
+
+const getRankTextClass = (rank: number) => {
+  if (rank === 1) {
+    return "text-white";
+  }
+
+  if (rank === 2 || rank === 3) {
+    return "text-tokhin-green";
+  }
+
+  return "text-stone-500";
+};
+
+const getRankBackgroundClass = (rank: number) => {
+  return rank === 1 ? "bg-tokhin-green" : "bg-neutral-50";
 };
 
 export default function LeaderboardPage() {
@@ -17,21 +54,82 @@
   const { session, isLoading: isSessionLoading } = useUserSession({
     requireAuth: true,
   });
-  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
+
+  const [activeTab, setActiveTab] = useState<LeaderboardTab>("BALANCE");
+  const [balanceLeaderboard, setBalanceLeaderboard] = useState<
+    LeaderboardBalanceItem[]
+  >([]);
+  const [roiLeaderboard, setRoiLeaderboard] = useState<LeaderboardRoiItem[]>([]);
   const [isLoading, setIsLoading] = useState(true);
+  const [errorMessage, setErrorMessage] = useState<string | null>(null);
 
   useEffect(() => {
-    if (!session) return;
+    if (!session?.user_id) {
+      return;
+    }
 
+    let isCancelled = false;
     const fetchLeaderboard = async () => {
       setIsLoading(true);
-      const data = await getLeaderboard("2025-10-05", "2025-12-31");
-      setLeaderboard(data);
-      setIsLoading(false);
+      setErrorMessage(null);
+
+      try {
+        const [balanceData, roiData] = await Promise.all([
+          getLeaderboardBalance(),
+          getLeaderboardRoi(),
+        ]);
+
+        if (isCancelled) {
+          return;
+        }
+
+        setBalanceLeaderboard(balanceData);
+        setRoiLeaderboard(roiData);
+      } catch (error) {
+        if (isCancelled) {
+          return;
+        }
+
+        const message =
+          error instanceof Error
+            ? error.message
+            : "리더보드 조회 중 오류가 발생했습니다";
+
+        setErrorMessage(message);
+        setBalanceLeaderboard([]);
+        setRoiLeaderboard([]);
+      } finally {
+        if (!isCancelled) {
+          setIsLoading(false);
+        }
+      }
     };
+
     fetchLeaderboard();
-  }, [session]);
 
+    return () => {
+      isCancelled = true;
+    };
+  }, [session?.user_id]);
+
+  const displayRows = useMemo<LeaderboardDisplayRow[]>(() => {
+    if (activeTab === "BALANCE") {
+      return balanceLeaderboard.map((entry) => ({
+        key: `balance-${entry.userId}`,
+        rank: entry.rank,
+        username: entry.username,
+        valueText: valueFormatter.format(entry.balance),
+      }));
+    }
+
+    return roiLeaderboard.map((entry) => ({
+      key: `roi-${entry.userId}`,
+      rank: entry.rank,
+      username: entry.username,
+      valueText: formatRoiPercent(entry.roiPercent),
+    }));
+  }, [activeTab, balanceLeaderboard, roiLeaderboard]);
+
   if (isSessionLoading) {
     return <p className="py-20 text-center text-zinc-500">로딩 중...</p>;
   }
@@ -47,63 +145,71 @@
           isMobile ? "text-xl" : "text-4xl"
         }`}
       >
-        실시간 순위
+        리더보드
       </h1>
 
+      <div className="mb-5 border-b border-zinc-200">
+        <div className="grid grid-cols-2">
+          {LEADERBOARD_TABS.map((tab) => (
+            <button
+              key={tab.id}
+              type="button"
+              onClick={() => setActiveTab(tab.id)}
+              className={`pb-3 text-sm font-semibold transition-colors ${
+                activeTab === tab.id
+                  ? "border-b-2 border-black text-black"
+                  : "text-zinc-400"
+              }`}
+            >
+              {tab.label}
+            </button>
+          ))}
+        </div>
+      </div>
+
       {isLoading ? (
         <p className="text-center">로딩 중...</p>
-      ) : leaderboard.length === 0 ? (
+      ) : errorMessage ? (
+        <p className="text-center text-red-500">{errorMessage}</p>
+      ) : displayRows.length === 0 ? (
         <p className="text-center text-gray-600">
-          아직 순위표를 볼 수 없습니다.
+          아직 {activeTab === "BALANCE" ? "잔고" : "수익률"} 순위표를 볼 수
+          없습니다.
         </p>
       ) : (
         <div>
           <ul className="divide-y divide-transparent">
-            {leaderboard.map((entry, index) => (
+            {displayRows.map((entry) => (
               <li
-                key={entry.userId}
+                key={entry.key}
                 className={`flex items-center ${
                   isMobile ? "px-4" : "px-4"
-                } h-12 ${
-                  index === 0 ? "bg-tokhin-green" : "bg-neutral-50"
-                } rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
+                } h-12 ${getRankBackgroundClass(
+                  entry.rank
+                )} rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
               >
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${
+                  className={`font-base ${getRankTextClass(entry.rank)} ${
                     isMobile
                       ? "text-base w-6 text-center"
                       : "text-lg w-12 text-center"
                   }`}
                 >
-                  {index + 1}
+                  {entry.rank}
                 </span>
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
+                  className={`font-base ${getRankTextClass(
+                    entry.rank
+                  )} flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
                 >
-                  {entry.name}
+                  {entry.username}
                 </span>
                 <span
-                  className={`font-normal ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${isMobile ? "text-base px-1" : "text-2xl"}`}
+                  className={`font-normal ${getRankTextClass(entry.rank)} ${
+                    isMobile ? "text-base px-1" : "text-2xl"
+                  }`}
                 >
-                  {entry.score}
+                  {entry.valueText}
                 </span>
               </li>
             ))}
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/prd.json b/prd.json
index 88f5357a539191312b41088c3cface270425eb93..79cc576c022136f0b53f7c46f944a6e697bb4e50
--- a/prd.json
+++ b/prd.json
@@ -124,7 +124,7 @@
       "id": "US-008",
       "title": "\ub9ac\ub354\ubcf4\ub4dc \u2014 \ucf54\uc778 \uc794\uace0 + \uc218\uc775\ub960 \ubcf5\ud569 \ub7ad\ud0b9",
       "priority": 8,
-      "passes": false,
+      "passes": true,
       "description": "\ub9ac\ub354\ubcf4\ub4dc\ub97c \ucf54\uc778 \uc794\uace0 \uc21c\uc704\uc640 \uc218\uc775\ub960 \uc21c\uc704 \ub450 \uac00\uc9c0 \ud0ed\uc73c\ub85c \uc81c\uacf5\ud55c\ub2e4. \ub514\uc790\uc778\uc740 prd.md\uc758 W-07 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ucc38\uc870.",
       "acceptanceCriteria": [
         "REQ-053 get_leaderboard_balance() RETURNS TABLE(...) RPC \ud568\uc218. \ucf54\uc778 \uc794\uace0 \uae30\uc900 \ub0b4\ub9bc\ucc28\uc21c \uc815\ub82c. \ubc18\ud658: rank, user_id, username, balance.",
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Inspecting new migration file**
exec
/bin/zsh -lc "sed -n '1,260p' supabase/migrations/20260224_us008_leaderboard_rankings.sql" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹

-- REQ-053: 코인 잔고 순위 조회
CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
RETURNS TABLE(
  rank BIGINT,
  user_id UUID,
  username TEXT,
  balance NUMERIC
)
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
BEGIN
  RETURN QUERY
  WITH leaderboard AS (
    SELECT
      u.id AS user_id,
      u.username,
file update:
diff --git a/app/leaderboard/page.tsx b/app/leaderboard/page.tsx
index 2adb2b80e2f8fed380614a6cbd203a475e0a18fa..cf6731cc7b5e350119555e4e2824b14c9a2f3f56
--- a/app/leaderboard/page.tsx
+++ b/app/leaderboard/page.tsx
@@ -1,15 +1,52 @@
 "use client";
 
-import { useState, useEffect } from "react";
-import { getLeaderboard } from "@/lib/api";
+import { useEffect, useMemo, useState } from "react";
+import {
+  getLeaderboardBalance,
+  getLeaderboardRoi,
+  type LeaderboardBalanceItem,
+  type LeaderboardRoiItem,
+} from "@/lib/api";
 import { useIsMobile } from "@/lib/hooks/useResponsive";
 import { useUserSession } from "@/lib/hooks/useUserSession";
 
-type LeaderboardEntry = {
-  userId: string;
-  name: string;
-  student_number: string;
-  score: number;
+type LeaderboardTab = "BALANCE" | "ROI";
+
+type LeaderboardDisplayRow = {
+  key: string;
+  rank: number;
+  username: string;
+  valueText: string;
+};
+
+const LEADERBOARD_TABS: Array<{ id: LeaderboardTab; label: string }> = [
+  { id: "BALANCE", label: "잔고 순위" },
+  { id: "ROI", label: "수익률 순위" },
+];
+
+const valueFormatter = new Intl.NumberFormat("ko-KR", {
+  maximumFractionDigits: 2,
+});
+
+const formatRoiPercent = (value: number) => {
+  const formatted = valueFormatter.format(Math.abs(value));
+  return `${value >= 0 ? "+" : "-"}${formatted}%`;
+};
+
+const getRankTextClass = (rank: number) => {
+  if (rank === 1) {
+    return "text-white";
+  }
+
+  if (rank === 2 || rank === 3) {
+    return "text-tokhin-green";
+  }
+
+  return "text-stone-500";
+};
+
+const getRankBackgroundClass = (rank: number) => {
+  return rank === 1 ? "bg-tokhin-green" : "bg-neutral-50";
 };
 
 export default function LeaderboardPage() {
@@ -17,21 +54,82 @@
   const { session, isLoading: isSessionLoading } = useUserSession({
     requireAuth: true,
   });
-  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
+
+  const [activeTab, setActiveTab] = useState<LeaderboardTab>("BALANCE");
+  const [balanceLeaderboard, setBalanceLeaderboard] = useState<
+    LeaderboardBalanceItem[]
+  >([]);
+  const [roiLeaderboard, setRoiLeaderboard] = useState<LeaderboardRoiItem[]>([]);
   const [isLoading, setIsLoading] = useState(true);
+  const [errorMessage, setErrorMessage] = useState<string | null>(null);
 
   useEffect(() => {
-    if (!session) return;
+    if (!session?.user_id) {
+      return;
+    }
 
+    let isCancelled = false;
     const fetchLeaderboard = async () => {
       setIsLoading(true);
-      const data = await getLeaderboard("2025-10-05", "2025-12-31");
-      setLeaderboard(data);
-      setIsLoading(false);
+      setErrorMessage(null);
+
+      try {
+        const [balanceData, roiData] = await Promise.all([
+          getLeaderboardBalance(),
+          getLeaderboardRoi(),
+        ]);
+
+        if (isCancelled) {
+          return;
+        }
+
+        setBalanceLeaderboard(balanceData);
+        setRoiLeaderboard(roiData);
+      } catch (error) {
+        if (isCancelled) {
+          return;
+        }
+
+        const message =
+          error instanceof Error
+            ? error.message
+            : "리더보드 조회 중 오류가 발생했습니다";
+
+        setErrorMessage(message);
+        setBalanceLeaderboard([]);
+        setRoiLeaderboard([]);
+      } finally {
+        if (!isCancelled) {
+          setIsLoading(false);
+        }
+      }
     };
+
     fetchLeaderboard();
-  }, [session]);
 
+    return () => {
+      isCancelled = true;
+    };
+  }, [session?.user_id]);
+
+  const displayRows = useMemo<LeaderboardDisplayRow[]>(() => {
+    if (activeTab === "BALANCE") {
+      return balanceLeaderboard.map((entry) => ({
+        key: `balance-${entry.userId}`,
+        rank: entry.rank,
+        username: entry.username,
+        valueText: valueFormatter.format(entry.balance),
+      }));
+    }
+
+    return roiLeaderboard.map((entry) => ({
+      key: `roi-${entry.userId}`,
+      rank: entry.rank,
+      username: entry.username,
+      valueText: formatRoiPercent(entry.roiPercent),
+    }));
+  }, [activeTab, balanceLeaderboard, roiLeaderboard]);
+
   if (isSessionLoading) {
     return <p className="py-20 text-center text-zinc-500">로딩 중...</p>;
   }
@@ -47,63 +145,71 @@
           isMobile ? "text-xl" : "text-4xl"
         }`}
       >
-        실시간 순위
+        리더보드
       </h1>
 
+      <div className="mb-5 border-b border-zinc-200">
+        <div className="grid grid-cols-2">
+          {LEADERBOARD_TABS.map((tab) => (
+            <button
+              key={tab.id}
+              type="button"
+              onClick={() => setActiveTab(tab.id)}
+              className={`pb-3 text-sm font-semibold transition-colors ${
+                activeTab === tab.id
+                  ? "border-b-2 border-black text-black"
+                  : "text-zinc-400"
+              }`}
+            >
+              {tab.label}
+            </button>
+          ))}
+        </div>
+      </div>
+
       {isLoading ? (
         <p className="text-center">로딩 중...</p>
-      ) : leaderboard.length === 0 ? (
+      ) : errorMessage ? (
+        <p className="text-center text-red-500">{errorMessage}</p>
+      ) : displayRows.length === 0 ? (
         <p className="text-center text-gray-600">
-          아직 순위표를 볼 수 없습니다.
+          아직 {activeTab === "BALANCE" ? "잔고" : "수익률"} 순위표를 볼 수
+          없습니다.
         </p>
       ) : (
         <div>
           <ul className="divide-y divide-transparent">
-            {leaderboard.map((entry, index) => (
+            {displayRows.map((entry) => (
               <li
-                key={entry.userId}
+                key={entry.key}
                 className={`flex items-center ${
                   isMobile ? "px-4" : "px-4"
-                } h-12 ${
-                  index === 0 ? "bg-tokhin-green" : "bg-neutral-50"
-                } rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
+                } h-12 ${getRankBackgroundClass(
+                  entry.rank
+                )} rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
               >
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${
+                  className={`font-base ${getRankTextClass(entry.rank)} ${
                     isMobile
                       ? "text-base w-6 text-center"
                       : "text-lg w-12 text-center"
                   }`}
                 >
-                  {index + 1}
+                  {entry.rank}
                 </span>
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
+                  className={`font-base ${getRankTextClass(
+                    entry.rank
+                  )} flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
                 >
-                  {entry.name}
+                  {entry.username}
                 </span>
                 <span
-                  className={`font-normal ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${isMobile ? "text-base px-1" : "text-2xl"}`}
+                  className={`font-normal ${getRankTextClass(entry.rank)} ${
+                    isMobile ? "text-base px-1" : "text-2xl"
+                  }`}
                 >
-                  {entry.score}
+                  {entry.valueText}
                 </span>
               </li>
             ))}
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/prd.json b/prd.json
index 88f5357a539191312b41088c3cface270425eb93..79cc576c022136f0b53f7c46f944a6e697bb4e50
--- a/prd.json
+++ b/prd.json
@@ -124,7 +124,7 @@
       "id": "US-008",
       "title": "\ub9ac\ub354\ubcf4\ub4dc \u2014 \ucf54\uc778 \uc794\uace0 + \uc218\uc775\ub960 \ubcf5\ud569 \ub7ad\ud0b9",
       "priority": 8,
-      "passes": false,
+      "passes": true,
       "description": "\ub9ac\ub354\ubcf4\ub4dc\ub97c \ucf54\uc778 \uc794\uace0 \uc21c\uc704\uc640 \uc218\uc775\ub960 \uc21c\uc704 \ub450 \uac00\uc9c0 \ud0ed\uc73c\ub85c \uc81c\uacf5\ud55c\ub2e4. \ub514\uc790\uc778\uc740 prd.md\uc758 W-07 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ucc38\uc870.",
       "acceptanceCriteria": [
         "REQ-053 get_leaderboard_balance() RETURNS TABLE(...) RPC \ud568\uc218. \ucf54\uc778 \uc794\uace0 \uae30\uc900 \ub0b4\ub9bc\ucc28\uc21c \uc815\ub82c. \ubc18\ud658: rank, user_id, username, balance.",
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Verifying cleanup and formatting**
exec
/bin/zsh -lc "rg -n \"getLeaderboard\\(|get_leaderboard\\b|getLeaderboardBalance|getLeaderboardRoi\" app lib -S" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
lib/api.ts:816:export const getLeaderboardBalance = async (): Promise<
lib/api.ts:836:export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
app/leaderboard/page.tsx:5:  getLeaderboardBalance,
app/leaderboard/page.tsx:6:  getLeaderboardRoi,
app/leaderboard/page.tsx:78:          getLeaderboardBalance(),
app/leaderboard/page.tsx:79:          getLeaderboardRoi(),
file update:
diff --git a/app/leaderboard/page.tsx b/app/leaderboard/page.tsx
index 2adb2b80e2f8fed380614a6cbd203a475e0a18fa..cf6731cc7b5e350119555e4e2824b14c9a2f3f56
--- a/app/leaderboard/page.tsx
+++ b/app/leaderboard/page.tsx
@@ -1,15 +1,52 @@
 "use client";
 
-import { useState, useEffect } from "react";
-import { getLeaderboard } from "@/lib/api";
+import { useEffect, useMemo, useState } from "react";
+import {
+  getLeaderboardBalance,
+  getLeaderboardRoi,
+  type LeaderboardBalanceItem,
+  type LeaderboardRoiItem,
+} from "@/lib/api";
 import { useIsMobile } from "@/lib/hooks/useResponsive";
 import { useUserSession } from "@/lib/hooks/useUserSession";
 
-type LeaderboardEntry = {
-  userId: string;
-  name: string;
-  student_number: string;
-  score: number;
+type LeaderboardTab = "BALANCE" | "ROI";
+
+type LeaderboardDisplayRow = {
+  key: string;
+  rank: number;
+  username: string;
+  valueText: string;
+};
+
+const LEADERBOARD_TABS: Array<{ id: LeaderboardTab; label: string }> = [
+  { id: "BALANCE", label: "잔고 순위" },
+  { id: "ROI", label: "수익률 순위" },
+];
+
+const valueFormatter = new Intl.NumberFormat("ko-KR", {
+  maximumFractionDigits: 2,
+});
+
+const formatRoiPercent = (value: number) => {
+  const formatted = valueFormatter.format(Math.abs(value));
+  return `${value >= 0 ? "+" : "-"}${formatted}%`;
+};
+
+const getRankTextClass = (rank: number) => {
+  if (rank === 1) {
+    return "text-white";
+  }
+
+  if (rank === 2 || rank === 3) {
+    return "text-tokhin-green";
+  }
+
+  return "text-stone-500";
+};
+
+const getRankBackgroundClass = (rank: number) => {
+  return rank === 1 ? "bg-tokhin-green" : "bg-neutral-50";
 };
 
 export default function LeaderboardPage() {
@@ -17,21 +54,82 @@
   const { session, isLoading: isSessionLoading } = useUserSession({
     requireAuth: true,
   });
-  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
+
+  const [activeTab, setActiveTab] = useState<LeaderboardTab>("BALANCE");
+  const [balanceLeaderboard, setBalanceLeaderboard] = useState<
+    LeaderboardBalanceItem[]
+  >([]);
+  const [roiLeaderboard, setRoiLeaderboard] = useState<LeaderboardRoiItem[]>([]);
   const [isLoading, setIsLoading] = useState(true);
+  const [errorMessage, setErrorMessage] = useState<string | null>(null);
 
   useEffect(() => {
-    if (!session) return;
+    if (!session?.user_id) {
+      return;
+    }
 
+    let isCancelled = false;
     const fetchLeaderboard = async () => {
       setIsLoading(true);
-      const data = await getLeaderboard("2025-10-05", "2025-12-31");
-      setLeaderboard(data);
-      setIsLoading(false);
+      setErrorMessage(null);
+
+      try {
+        const [balanceData, roiData] = await Promise.all([
+          getLeaderboardBalance(),
+          getLeaderboardRoi(),
+        ]);
+
+        if (isCancelled) {
+          return;
+        }
+
+        setBalanceLeaderboard(balanceData);
+        setRoiLeaderboard(roiData);
+      } catch (error) {
+        if (isCancelled) {
+          return;
+        }
+
+        const message =
+          error instanceof Error
+            ? error.message
+            : "리더보드 조회 중 오류가 발생했습니다";
+
+        setErrorMessage(message);
+        setBalanceLeaderboard([]);
+        setRoiLeaderboard([]);
+      } finally {
+        if (!isCancelled) {
+          setIsLoading(false);
+        }
+      }
     };
+
     fetchLeaderboard();
-  }, [session]);
 
+    return () => {
+      isCancelled = true;
+    };
+  }, [session?.user_id]);
+
+  const displayRows = useMemo<LeaderboardDisplayRow[]>(() => {
+    if (activeTab === "BALANCE") {
+      return balanceLeaderboard.map((entry) => ({
+        key: `balance-${entry.userId}`,
+        rank: entry.rank,
+        username: entry.username,
+        valueText: valueFormatter.format(entry.balance),
+      }));
+    }
+
+    return roiLeaderboard.map((entry) => ({
+      key: `roi-${entry.userId}`,
+      rank: entry.rank,
+      username: entry.username,
+      valueText: formatRoiPercent(entry.roiPercent),
+    }));
+  }, [activeTab, balanceLeaderboard, roiLeaderboard]);
+
   if (isSessionLoading) {
     return <p className="py-20 text-center text-zinc-500">로딩 중...</p>;
   }
@@ -47,63 +145,71 @@
           isMobile ? "text-xl" : "text-4xl"
         }`}
       >
-        실시간 순위
+        리더보드
       </h1>
 
+      <div className="mb-5 border-b border-zinc-200">
+        <div className="grid grid-cols-2">
+          {LEADERBOARD_TABS.map((tab) => (
+            <button
+              key={tab.id}
+              type="button"
+              onClick={() => setActiveTab(tab.id)}
+              className={`pb-3 text-sm font-semibold transition-colors ${
+                activeTab === tab.id
+                  ? "border-b-2 border-black text-black"
+                  : "text-zinc-400"
+              }`}
+            >
+              {tab.label}
+            </button>
+          ))}
+        </div>
+      </div>
+
       {isLoading ? (
         <p className="text-center">로딩 중...</p>
-      ) : leaderboard.length === 0 ? (
+      ) : errorMessage ? (
+        <p className="text-center text-red-500">{errorMessage}</p>
+      ) : displayRows.length === 0 ? (
         <p className="text-center text-gray-600">
-          아직 순위표를 볼 수 없습니다.
+          아직 {activeTab === "BALANCE" ? "잔고" : "수익률"} 순위표를 볼 수
+          없습니다.
         </p>
       ) : (
         <div>
           <ul className="divide-y divide-transparent">
-            {leaderboard.map((entry, index) => (
+            {displayRows.map((entry) => (
               <li
-                key={entry.userId}
+                key={entry.key}
                 className={`flex items-center ${
                   isMobile ? "px-4" : "px-4"
-                } h-12 ${
-                  index === 0 ? "bg-tokhin-green" : "bg-neutral-50"
-                } rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
+                } h-12 ${getRankBackgroundClass(
+                  entry.rank
+                )} rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
               >
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${
+                  className={`font-base ${getRankTextClass(entry.rank)} ${
                     isMobile
                       ? "text-base w-6 text-center"
                       : "text-lg w-12 text-center"
                   }`}
                 >
-                  {index + 1}
+                  {entry.rank}
                 </span>
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
+                  className={`font-base ${getRankTextClass(
+                    entry.rank
+                  )} flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
                 >
-                  {entry.name}
+                  {entry.username}
                 </span>
                 <span
-                  className={`font-normal ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${isMobile ? "text-base px-1" : "text-2xl"}`}
+                  className={`font-normal ${getRankTextClass(entry.rank)} ${
+                    isMobile ? "text-base px-1" : "text-2xl"
+                  }`}
                 >
-                  {entry.score}
+                  {entry.valueText}
                 </span>
               </li>
             ))}
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/prd.json b/prd.json
index 88f5357a539191312b41088c3cface270425eb93..79cc576c022136f0b53f7c46f944a6e697bb4e50
--- a/prd.json
+++ b/prd.json
@@ -124,7 +124,7 @@
       "id": "US-008",
       "title": "\ub9ac\ub354\ubcf4\ub4dc \u2014 \ucf54\uc778 \uc794\uace0 + \uc218\uc775\ub960 \ubcf5\ud569 \ub7ad\ud0b9",
       "priority": 8,
-      "passes": false,
+      "passes": true,
       "description": "\ub9ac\ub354\ubcf4\ub4dc\ub97c \ucf54\uc778 \uc794\uace0 \uc21c\uc704\uc640 \uc218\uc775\ub960 \uc21c\uc704 \ub450 \uac00\uc9c0 \ud0ed\uc73c\ub85c \uc81c\uacf5\ud55c\ub2e4. \ub514\uc790\uc778\uc740 prd.md\uc758 W-07 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ucc38\uc870.",
       "acceptanceCriteria": [
         "REQ-053 get_leaderboard_balance() RETURNS TABLE(...) RPC \ud568\uc218. \ucf54\uc778 \uc794\uace0 \uae30\uc900 \ub0b4\ub9bc\ucc28\uc21c \uc815\ub82c. \ubc18\ud658: rank, user_id, username, balance.",
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Validating JSON format**
exec
/bin/zsh -lc "node -e \"JSON.parse(require('fs').readFileSync('prd.json','utf8')); console.log('prd.json ok')\"" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
prd.json ok
file update:
diff --git a/app/leaderboard/page.tsx b/app/leaderboard/page.tsx
index 2adb2b80e2f8fed380614a6cbd203a475e0a18fa..cf6731cc7b5e350119555e4e2824b14c9a2f3f56
--- a/app/leaderboard/page.tsx
+++ b/app/leaderboard/page.tsx
@@ -1,15 +1,52 @@
 "use client";
 
-import { useState, useEffect } from "react";
-import { getLeaderboard } from "@/lib/api";
+import { useEffect, useMemo, useState } from "react";
+import {
+  getLeaderboardBalance,
+  getLeaderboardRoi,
+  type LeaderboardBalanceItem,
+  type LeaderboardRoiItem,
+} from "@/lib/api";
 import { useIsMobile } from "@/lib/hooks/useResponsive";
 import { useUserSession } from "@/lib/hooks/useUserSession";
 
-type LeaderboardEntry = {
-  userId: string;
-  name: string;
-  student_number: string;
-  score: number;
+type LeaderboardTab = "BALANCE" | "ROI";
+
+type LeaderboardDisplayRow = {
+  key: string;
+  rank: number;
+  username: string;
+  valueText: string;
+};
+
+const LEADERBOARD_TABS: Array<{ id: LeaderboardTab; label: string }> = [
+  { id: "BALANCE", label: "잔고 순위" },
+  { id: "ROI", label: "수익률 순위" },
+];
+
+const valueFormatter = new Intl.NumberFormat("ko-KR", {
+  maximumFractionDigits: 2,
+});
+
+const formatRoiPercent = (value: number) => {
+  const formatted = valueFormatter.format(Math.abs(value));
+  return `${value >= 0 ? "+" : "-"}${formatted}%`;
+};
+
+const getRankTextClass = (rank: number) => {
+  if (rank === 1) {
+    return "text-white";
+  }
+
+  if (rank === 2 || rank === 3) {
+    return "text-tokhin-green";
+  }
+
+  return "text-stone-500";
+};
+
+const getRankBackgroundClass = (rank: number) => {
+  return rank === 1 ? "bg-tokhin-green" : "bg-neutral-50";
 };
 
 export default function LeaderboardPage() {
@@ -17,21 +54,82 @@
   const { session, isLoading: isSessionLoading } = useUserSession({
     requireAuth: true,
   });
-  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
+
+  const [activeTab, setActiveTab] = useState<LeaderboardTab>("BALANCE");
+  const [balanceLeaderboard, setBalanceLeaderboard] = useState<
+    LeaderboardBalanceItem[]
+  >([]);
+  const [roiLeaderboard, setRoiLeaderboard] = useState<LeaderboardRoiItem[]>([]);
   const [isLoading, setIsLoading] = useState(true);
+  const [errorMessage, setErrorMessage] = useState<string | null>(null);
 
   useEffect(() => {
-    if (!session) return;
+    if (!session?.user_id) {
+      return;
+    }
 
+    let isCancelled = false;
     const fetchLeaderboard = async () => {
       setIsLoading(true);
-      const data = await getLeaderboard("2025-10-05", "2025-12-31");
-      setLeaderboard(data);
-      setIsLoading(false);
+      setErrorMessage(null);
+
+      try {
+        const [balanceData, roiData] = await Promise.all([
+          getLeaderboardBalance(),
+          getLeaderboardRoi(),
+        ]);
+
+        if (isCancelled) {
+          return;
+        }
+
+        setBalanceLeaderboard(balanceData);
+        setRoiLeaderboard(roiData);
+      } catch (error) {
+        if (isCancelled) {
+          return;
+        }
+
+        const message =
+          error instanceof Error
+            ? error.message
+            : "리더보드 조회 중 오류가 발생했습니다";
+
+        setErrorMessage(message);
+        setBalanceLeaderboard([]);
+        setRoiLeaderboard([]);
+      } finally {
+        if (!isCancelled) {
+          setIsLoading(false);
+        }
+      }
     };
+
     fetchLeaderboard();
-  }, [session]);
 
+    return () => {
+      isCancelled = true;
+    };
+  }, [session?.user_id]);
+
+  const displayRows = useMemo<LeaderboardDisplayRow[]>(() => {
+    if (activeTab === "BALANCE") {
+      return balanceLeaderboard.map((entry) => ({
+        key: `balance-${entry.userId}`,
+        rank: entry.rank,
+        username: entry.username,
+        valueText: valueFormatter.format(entry.balance),
+      }));
+    }
+
+    return roiLeaderboard.map((entry) => ({
+      key: `roi-${entry.userId}`,
+      rank: entry.rank,
+      username: entry.username,
+      valueText: formatRoiPercent(entry.roiPercent),
+    }));
+  }, [activeTab, balanceLeaderboard, roiLeaderboard]);
+
   if (isSessionLoading) {
     return <p className="py-20 text-center text-zinc-500">로딩 중...</p>;
   }
@@ -47,63 +145,71 @@
           isMobile ? "text-xl" : "text-4xl"
         }`}
       >
-        실시간 순위
+        리더보드
       </h1>
 
+      <div className="mb-5 border-b border-zinc-200">
+        <div className="grid grid-cols-2">
+          {LEADERBOARD_TABS.map((tab) => (
+            <button
+              key={tab.id}
+              type="button"
+              onClick={() => setActiveTab(tab.id)}
+              className={`pb-3 text-sm font-semibold transition-colors ${
+                activeTab === tab.id
+                  ? "border-b-2 border-black text-black"
+                  : "text-zinc-400"
+              }`}
+            >
+              {tab.label}
+            </button>
+          ))}
+        </div>
+      </div>
+
       {isLoading ? (
         <p className="text-center">로딩 중...</p>
-      ) : leaderboard.length === 0 ? (
+      ) : errorMessage ? (
+        <p className="text-center text-red-500">{errorMessage}</p>
+      ) : displayRows.length === 0 ? (
         <p className="text-center text-gray-600">
-          아직 순위표를 볼 수 없습니다.
+          아직 {activeTab === "BALANCE" ? "잔고" : "수익률"} 순위표를 볼 수
+          없습니다.
         </p>
       ) : (
         <div>
           <ul className="divide-y divide-transparent">
-            {leaderboard.map((entry, index) => (
+            {displayRows.map((entry) => (
               <li
-                key={entry.userId}
+                key={entry.key}
                 className={`flex items-center ${
                   isMobile ? "px-4" : "px-4"
-                } h-12 ${
-                  index === 0 ? "bg-tokhin-green" : "bg-neutral-50"
-                } rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
+                } h-12 ${getRankBackgroundClass(
+                  entry.rank
+                )} rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
               >
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${
+                  className={`font-base ${getRankTextClass(entry.rank)} ${
                     isMobile
                       ? "text-base w-6 text-center"
                       : "text-lg w-12 text-center"
                   }`}
                 >
-                  {index + 1}
+                  {entry.rank}
                 </span>
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
+                  className={`font-base ${getRankTextClass(
+                    entry.rank
+                  )} flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
                 >
-                  {entry.name}
+                  {entry.username}
                 </span>
                 <span
-                  className={`font-normal ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${isMobile ? "text-base px-1" : "text-2xl"}`}
+                  className={`font-normal ${getRankTextClass(entry.rank)} ${
+                    isMobile ? "text-base px-1" : "text-2xl"
+                  }`}
                 >
-                  {entry.score}
+                  {entry.valueText}
                 </span>
               </li>
             ))}
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/prd.json b/prd.json
index 88f5357a539191312b41088c3cface270425eb93..79cc576c022136f0b53f7c46f944a6e697bb4e50
--- a/prd.json
+++ b/prd.json
@@ -124,7 +124,7 @@
       "id": "US-008",
       "title": "\ub9ac\ub354\ubcf4\ub4dc \u2014 \ucf54\uc778 \uc794\uace0 + \uc218\uc775\ub960 \ubcf5\ud569 \ub7ad\ud0b9",
       "priority": 8,
-      "passes": false,
+      "passes": true,
       "description": "\ub9ac\ub354\ubcf4\ub4dc\ub97c \ucf54\uc778 \uc794\uace0 \uc21c\uc704\uc640 \uc218\uc775\ub960 \uc21c\uc704 \ub450 \uac00\uc9c0 \ud0ed\uc73c\ub85c \uc81c\uacf5\ud55c\ub2e4. \ub514\uc790\uc778\uc740 prd.md\uc758 W-07 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ucc38\uc870.",
       "acceptanceCriteria": [
         "REQ-053 get_leaderboard_balance() RETURNS TABLE(...) RPC \ud568\uc218. \ucf54\uc778 \uc794\uace0 \uae30\uc900 \ub0b4\ub9bc\ucc28\uc21c \uc815\ub82c. \ubc18\ud658: rank, user_id, username, balance.",
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Validating SQL ranks and styling**
Plan update
  ✓ Inspect current leaderboard data flow and identify exact files/migrations to update for US-008.
  ✓ Add Supabase migration for REQ-053/REQ-054 RPC functions and grants.
  ✓ Update lib/api.ts with new leaderboard RPC client functions and types.
  ✓ Refactor app/leaderboard/page.tsx to support balance/ROI tab switching with required ranking styles.
  ✓ Run verification (lint/build subset) and update prd.json story status for US-008 if all criteria are met.
file update:
diff --git a/app/leaderboard/page.tsx b/app/leaderboard/page.tsx
index 2adb2b80e2f8fed380614a6cbd203a475e0a18fa..cf6731cc7b5e350119555e4e2824b14c9a2f3f56
--- a/app/leaderboard/page.tsx
+++ b/app/leaderboard/page.tsx
@@ -1,15 +1,52 @@
 "use client";
 
-import { useState, useEffect } from "react";
-import { getLeaderboard } from "@/lib/api";
+import { useEffect, useMemo, useState } from "react";
+import {
+  getLeaderboardBalance,
+  getLeaderboardRoi,
+  type LeaderboardBalanceItem,
+  type LeaderboardRoiItem,
+} from "@/lib/api";
 import { useIsMobile } from "@/lib/hooks/useResponsive";
 import { useUserSession } from "@/lib/hooks/useUserSession";
 
-type LeaderboardEntry = {
-  userId: string;
-  name: string;
-  student_number: string;
-  score: number;
+type LeaderboardTab = "BALANCE" | "ROI";
+
+type LeaderboardDisplayRow = {
+  key: string;
+  rank: number;
+  username: string;
+  valueText: string;
+};
+
+const LEADERBOARD_TABS: Array<{ id: LeaderboardTab; label: string }> = [
+  { id: "BALANCE", label: "잔고 순위" },
+  { id: "ROI", label: "수익률 순위" },
+];
+
+const valueFormatter = new Intl.NumberFormat("ko-KR", {
+  maximumFractionDigits: 2,
+});
+
+const formatRoiPercent = (value: number) => {
+  const formatted = valueFormatter.format(Math.abs(value));
+  return `${value >= 0 ? "+" : "-"}${formatted}%`;
+};
+
+const getRankTextClass = (rank: number) => {
+  if (rank === 1) {
+    return "text-white";
+  }
+
+  if (rank === 2 || rank === 3) {
+    return "text-tokhin-green";
+  }
+
+  return "text-stone-500";
+};
+
+const getRankBackgroundClass = (rank: number) => {
+  return rank === 1 ? "bg-tokhin-green" : "bg-neutral-50";
 };
 
 export default function LeaderboardPage() {
@@ -17,21 +54,82 @@
   const { session, isLoading: isSessionLoading } = useUserSession({
     requireAuth: true,
   });
-  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
+
+  const [activeTab, setActiveTab] = useState<LeaderboardTab>("BALANCE");
+  const [balanceLeaderboard, setBalanceLeaderboard] = useState<
+    LeaderboardBalanceItem[]
+  >([]);
+  const [roiLeaderboard, setRoiLeaderboard] = useState<LeaderboardRoiItem[]>([]);
   const [isLoading, setIsLoading] = useState(true);
+  const [errorMessage, setErrorMessage] = useState<string | null>(null);
 
   useEffect(() => {
-    if (!session) return;
+    if (!session?.user_id) {
+      return;
+    }
 
+    let isCancelled = false;
     const fetchLeaderboard = async () => {
       setIsLoading(true);
-      const data = await getLeaderboard("2025-10-05", "2025-12-31");
-      setLeaderboard(data);
-      setIsLoading(false);
+      setErrorMessage(null);
+
+      try {
+        const [balanceData, roiData] = await Promise.all([
+          getLeaderboardBalance(),
+          getLeaderboardRoi(),
+        ]);
+
+        if (isCancelled) {
+          return;
+        }
+
+        setBalanceLeaderboard(balanceData);
+        setRoiLeaderboard(roiData);
+      } catch (error) {
+        if (isCancelled) {
+          return;
+        }
+
+        const message =
+          error instanceof Error
+            ? error.message
+            : "리더보드 조회 중 오류가 발생했습니다";
+
+        setErrorMessage(message);
+        setBalanceLeaderboard([]);
+        setRoiLeaderboard([]);
+      } finally {
+        if (!isCancelled) {
+          setIsLoading(false);
+        }
+      }
     };
+
     fetchLeaderboard();
-  }, [session]);
 
+    return () => {
+      isCancelled = true;
+    };
+  }, [session?.user_id]);
+
+  const displayRows = useMemo<LeaderboardDisplayRow[]>(() => {
+    if (activeTab === "BALANCE") {
+      return balanceLeaderboard.map((entry) => ({
+        key: `balance-${entry.userId}`,
+        rank: entry.rank,
+        username: entry.username,
+        valueText: valueFormatter.format(entry.balance),
+      }));
+    }
+
+    return roiLeaderboard.map((entry) => ({
+      key: `roi-${entry.userId}`,
+      rank: entry.rank,
+      username: entry.username,
+      valueText: formatRoiPercent(entry.roiPercent),
+    }));
+  }, [activeTab, balanceLeaderboard, roiLeaderboard]);
+
   if (isSessionLoading) {
     return <p className="py-20 text-center text-zinc-500">로딩 중...</p>;
   }
@@ -47,63 +145,71 @@
           isMobile ? "text-xl" : "text-4xl"
         }`}
       >
-        실시간 순위
+        리더보드
       </h1>
 
+      <div className="mb-5 border-b border-zinc-200">
+        <div className="grid grid-cols-2">
+          {LEADERBOARD_TABS.map((tab) => (
+            <button
+              key={tab.id}
+              type="button"
+              onClick={() => setActiveTab(tab.id)}
+              className={`pb-3 text-sm font-semibold transition-colors ${
+                activeTab === tab.id
+                  ? "border-b-2 border-black text-black"
+                  : "text-zinc-400"
+              }`}
+            >
+              {tab.label}
+            </button>
+          ))}
+        </div>
+      </div>
+
       {isLoading ? (
         <p className="text-center">로딩 중...</p>
-      ) : leaderboard.length === 0 ? (
+      ) : errorMessage ? (
+        <p className="text-center text-red-500">{errorMessage}</p>
+      ) : displayRows.length === 0 ? (
         <p className="text-center text-gray-600">
-          아직 순위표를 볼 수 없습니다.
+          아직 {activeTab === "BALANCE" ? "잔고" : "수익률"} 순위표를 볼 수
+          없습니다.
         </p>
       ) : (
         <div>
           <ul className="divide-y divide-transparent">
-            {leaderboard.map((entry, index) => (
+            {displayRows.map((entry) => (
               <li
-                key={entry.userId}
+                key={entry.key}
                 className={`flex items-center ${
                   isMobile ? "px-4" : "px-4"
-                } h-12 ${
-                  index === 0 ? "bg-tokhin-green" : "bg-neutral-50"
-                } rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
+                } h-12 ${getRankBackgroundClass(
+                  entry.rank
+                )} rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
               >
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${
+                  className={`font-base ${getRankTextClass(entry.rank)} ${
                     isMobile
                       ? "text-base w-6 text-center"
                       : "text-lg w-12 text-center"
                   }`}
                 >
-                  {index + 1}
+                  {entry.rank}
                 </span>
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
+                  className={`font-base ${getRankTextClass(
+                    entry.rank
+                  )} flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
                 >
-                  {entry.name}
+                  {entry.username}
                 </span>
                 <span
-                  className={`font-normal ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${isMobile ? "text-base px-1" : "text-2xl"}`}
+                  className={`font-normal ${getRankTextClass(entry.rank)} ${
+                    isMobile ? "text-base px-1" : "text-2xl"
+                  }`}
                 >
-                  {entry.score}
+                  {entry.valueText}
                 </span>
               </li>
             ))}
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/prd.json b/prd.json
index 88f5357a539191312b41088c3cface270425eb93..79cc576c022136f0b53f7c46f944a6e697bb4e50
--- a/prd.json
+++ b/prd.json
@@ -124,7 +124,7 @@
       "id": "US-008",
       "title": "\ub9ac\ub354\ubcf4\ub4dc \u2014 \ucf54\uc778 \uc794\uace0 + \uc218\uc775\ub960 \ubcf5\ud569 \ub7ad\ud0b9",
       "priority": 8,
-      "passes": false,
+      "passes": true,
       "description": "\ub9ac\ub354\ubcf4\ub4dc\ub97c \ucf54\uc778 \uc794\uace0 \uc21c\uc704\uc640 \uc218\uc775\ub960 \uc21c\uc704 \ub450 \uac00\uc9c0 \ud0ed\uc73c\ub85c \uc81c\uacf5\ud55c\ub2e4. \ub514\uc790\uc778\uc740 prd.md\uc758 W-07 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ucc38\uc870.",
       "acceptanceCriteria": [
         "REQ-053 get_leaderboard_balance() RETURNS TABLE(...) RPC \ud568\uc218. \ucf54\uc778 \uc794\uace0 \uae30\uc900 \ub0b4\ub9bc\ucc28\uc21c \uc815\ub82c. \ubc18\ud658: rank, user_id, username, balance.",
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Noting untracked files and line references**
codex
검증 명령(`lint`, `build`)까지 완료했고 모두 통과했습니다. 마지막으로 변경 파일/라인을 정리해서 전달하겠습니다.
exec
/bin/zsh -lc "nl -ba supabase/migrations/20260224_us008_leaderboard_rankings.sql | sed -n '1,260p'" in /Users/jeffrey/WebstormProjects/lookin/lookinexec
/bin/zsh -lc "nl -ba lib/api.ts | sed -n '160,320p'" in /Users/jeffrey/WebstormProjects/lookin/lookinexec
/bin/zsh -lc "nl -ba app/leaderboard/page.tsx | sed -n '1,260p'" in /Users/jeffrey/WebstormProjects/lookin/lookinexec
/bin/zsh -lc "nl -ba lib/api.ts | sed -n '800,880p'" in /Users/jeffrey/WebstormProjects/lookin/lookinexec
/bin/zsh -lc "nl -ba prd.json | sed -n '118,142p'" in /Users/jeffrey/WebstormProjects/lookin/lookin succeeded in 52ms:
   160	  game_id: number;
   161	  status: string;
   162	  result: string | null;
   163	}
   164	
   165	interface GameForHistoryContextRow {
   166	  id: number;
   167	  game_date: string;
   168	  game_time: string | null;
   169	  home_team: TeamRelation;
   170	  away_team: TeamRelation;
   171	}
   172	
   173	interface LeaderboardBalanceRpcRow {
   174	  rank: number | string;
   175	  user_id: string;
   176	  username: string;
   177	  balance: number | string;
   178	}
   179	
 succeeded in 52ms:
     1	-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
     2	
     3	-- REQ-053: 코인 잔고 순위 조회
     4	CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
     5	RETURNS TABLE(
     6	  rank BIGINT,
     7	  user_id UUID,
     8	  username TEXT,
     9	  balance NUMERIC
    10	)
    11	LANGUAGE plpgsql
    12	SECURITY DEFINER
    13	SET search_path = public
    14	AS $$
    15	BEGIN
    16	  RETURN QUERY
    17	  WITH leaderboard AS (
    18	    SELECT
    19	      u.id AS user_id,
    20	      u.username,
 succeeded in 51ms:
     1	"use client";
     2	
     3	import { useEffect, useMemo, useState } from "react";
     4	import {
     5	  getLeaderboardBalance,
     6	  getLeaderboardRoi,
     7	  type LeaderboardBalanceItem,
     8	  type LeaderboardRoiItem,
     9	} from "@/lib/api";
    10	import { useIsMobile } from "@/lib/hooks/useResponsive";
    11	import { useUserSession } from "@/lib/hooks/useUserSession";
    12	
    13	type LeaderboardTab = "BALANCE" | "ROI";
    14	
    15	type LeaderboardDisplayRow = {
    16	  key: string;
    17	  rank: number;
    18	  username: string;
    19	  valueText: string;
    20	};
 succeeded in 51ms:
   800	                : awayTeam.name,
   801	            is_correct:
   802	              actual_winner_id !== null
   803	                ? prediction.predicted_winner_team_id === actual_winner_id
   804	                : false,
   805	            points_earned: prediction.points_earned || 0,
   806	            is_settled: prediction.is_settled || false,
   807	          }
   808	        : null,
   809	    };
   810	  });
   811	
   812	  return { games: combinedData, totalPoints };
   813	};
   814	
   815	// 5. Fetch Leaderboard (US-008)
   816	export const getLeaderboardBalance = async (): Promise<
   817	  LeaderboardBalanceItem[]
   818	> => {
   819	  const { data, error } = await supabase.rpc("get_leaderboard_balance");
 succeeded in 51ms:
   118	        "REQ-050 '\uac70\ub798 \ub0b4\uc5ed' \ud0ed: \ucd5c\uadfc \uc8fc\ubb38 \ubaa9\ub85d (\ub0a0\uc9dc\ubcc4 \uadf8\ub8f9\ud551). \uac01 \ud56d\ubaa9: \ub0a0\uc9dc/\uc2dc\uac04, \uacbd\uae30 \uc815\ubcf4, BUY(\ucd08\ub85d)/SELL(\ube68\uac15), outcome, \uc218\ub7c9, \uccb4\uacb0\uae08\uc561.",
   119	        "REQ-051 '\uc815\uc0b0 \ub0b4\uc5ed' \ud0ed: \uc815\uc0b0 \uc644\ub8cc\ub41c \ub9c8\ucf13\ubcc4 \uc190\uc775. \uac01 \ud56d\ubaa9: \uacbd\uae30 \uc815\ubcf4, \ubcf4\uc720\ud588\ub358 \ud3ec\uc9c0\uc158, \uc815\uc0b0\uae08\uc561, \ucd5c\uc885 \uc190\uc775.",
   120	        "REQ-052 \uae30\uc874 \uce98\ub9b0\ub354 \uae30\ubc18 \ub0a0\uc9dc \ub124\ube44\uac8c\uc774\uc158 UI \uc720\uc9c0 (\uac70\ub798\ub0b4\uc5ed \ud544\ud130\ub9c1\uc6a9)."
   121	      ]
   122	    },
   123	    {
   124	      "id": "US-008",
   125	      "title": "\ub9ac\ub354\ubcf4\ub4dc \u2014 \ucf54\uc778 \uc794\uace0 + \uc218\uc775\ub960 \ubcf5\ud569 \ub7ad\ud0b9",
   126	      "priority": 8,
   127	      "passes": true,
   128	      "description": "\ub9ac\ub354\ubcf4\ub4dc\ub97c \ucf54\uc778 \uc794\uace0 \uc21c\uc704\uc640 \uc218\uc775\ub960 \uc21c\uc704 \ub450 \uac00\uc9c0 \ud0ed\uc73c\ub85c \uc81c\uacf5\ud55c\ub2e4. \ub514\uc790\uc778\uc740 prd.md\uc758 W-07 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ucc38\uc870.",
   129	      "acceptanceCriteria": [
   130	        "REQ-053 get_leaderboard_balance() RETURNS TABLE(...) RPC \ud568\uc218. \ucf54\uc778 \uc794\uace0 \uae30\uc900 \ub0b4\ub9bc\ucc28\uc21c \uc815\ub82c. \ubc18\ud658: rank, user_id, username, balance.",
   131	        "REQ-054 get_leaderboard_roi() RETURNS TABLE(...) RPC \ud568\uc218. \uc218\uc775\ub960 = (\ud604\uc7ac\uc794\uace0 - \ub204\uc801\uc9c0\uae09\ucf54\uc778) / \ub204\uc801\uc9c0\uae09\ucf54\uc778 \u00d7 100. \ubc18\ud658: rank, user_id, username, roi_percent, current_balance, total_granted.",
   132	        "REQ-055 \ub9ac\ub354\ubcf4\ub4dc UI\uc5d0 '\uc794\uace0 \uc21c\uc704' / '\uc218\uc775\ub960 \uc21c\uc704' \ud0ed \uc804\ud658.",
   133	        "REQ-056 \uae30\uc874 \ub9ac\ub354\ubcf4\ub4dc \uc2a4\ud0c0\uc77c \uc720\uc9c0: 1\ub4f1 tokhin-green \ubc30\uacbd + \ud770 \ud14d\uc2a4\ud2b8, 2~3\ub4f1 tokhin-green \ud14d\uc2a4\ud2b8, \ub098\uba38\uc9c0 \ud68c\uc0c9."
   134	      ]
   135	    },
   136	    {
   137	      "id": "US-009",
file update:
diff --git a/app/leaderboard/page.tsx b/app/leaderboard/page.tsx
index 2adb2b80e2f8fed380614a6cbd203a475e0a18fa..cf6731cc7b5e350119555e4e2824b14c9a2f3f56
--- a/app/leaderboard/page.tsx
+++ b/app/leaderboard/page.tsx
@@ -1,15 +1,52 @@
 "use client";
 
-import { useState, useEffect } from "react";
-import { getLeaderboard } from "@/lib/api";
+import { useEffect, useMemo, useState } from "react";
+import {
+  getLeaderboardBalance,
+  getLeaderboardRoi,
+  type LeaderboardBalanceItem,
+  type LeaderboardRoiItem,
+} from "@/lib/api";
 import { useIsMobile } from "@/lib/hooks/useResponsive";
 import { useUserSession } from "@/lib/hooks/useUserSession";
 
-type LeaderboardEntry = {
-  userId: string;
-  name: string;
-  student_number: string;
-  score: number;
+type LeaderboardTab = "BALANCE" | "ROI";
+
+type LeaderboardDisplayRow = {
+  key: string;
+  rank: number;
+  username: string;
+  valueText: string;
+};
+
+const LEADERBOARD_TABS: Array<{ id: LeaderboardTab; label: string }> = [
+  { id: "BALANCE", label: "잔고 순위" },
+  { id: "ROI", label: "수익률 순위" },
+];
+
+const valueFormatter = new Intl.NumberFormat("ko-KR", {
+  maximumFractionDigits: 2,
+});
+
+const formatRoiPercent = (value: number) => {
+  const formatted = valueFormatter.format(Math.abs(value));
+  return `${value >= 0 ? "+" : "-"}${formatted}%`;
+};
+
+const getRankTextClass = (rank: number) => {
+  if (rank === 1) {
+    return "text-white";
+  }
+
+  if (rank === 2 || rank === 3) {
+    return "text-tokhin-green";
+  }
+
+  return "text-stone-500";
+};
+
+const getRankBackgroundClass = (rank: number) => {
+  return rank === 1 ? "bg-tokhin-green" : "bg-neutral-50";
 };
 
 export default function LeaderboardPage() {
@@ -17,21 +54,82 @@
   const { session, isLoading: isSessionLoading } = useUserSession({
     requireAuth: true,
   });
-  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
+
+  const [activeTab, setActiveTab] = useState<LeaderboardTab>("BALANCE");
+  const [balanceLeaderboard, setBalanceLeaderboard] = useState<
+    LeaderboardBalanceItem[]
+  >([]);
+  const [roiLeaderboard, setRoiLeaderboard] = useState<LeaderboardRoiItem[]>([]);
   const [isLoading, setIsLoading] = useState(true);
+  const [errorMessage, setErrorMessage] = useState<string | null>(null);
 
   useEffect(() => {
-    if (!session) return;
+    if (!session?.user_id) {
+      return;
+    }
 
+    let isCancelled = false;
     const fetchLeaderboard = async () => {
       setIsLoading(true);
-      const data = await getLeaderboard("2025-10-05", "2025-12-31");
-      setLeaderboard(data);
-      setIsLoading(false);
+      setErrorMessage(null);
+
+      try {
+        const [balanceData, roiData] = await Promise.all([
+          getLeaderboardBalance(),
+          getLeaderboardRoi(),
+        ]);
+
+        if (isCancelled) {
+          return;
+        }
+
+        setBalanceLeaderboard(balanceData);
+        setRoiLeaderboard(roiData);
+      } catch (error) {
+        if (isCancelled) {
+          return;
+        }
+
+        const message =
+          error instanceof Error
+            ? error.message
+            : "리더보드 조회 중 오류가 발생했습니다";
+
+        setErrorMessage(message);
+        setBalanceLeaderboard([]);
+        setRoiLeaderboard([]);
+      } finally {
+        if (!isCancelled) {
+          setIsLoading(false);
+        }
+      }
     };
+
     fetchLeaderboard();
-  }, [session]);
 
+    return () => {
+      isCancelled = true;
+    };
+  }, [session?.user_id]);
+
+  const displayRows = useMemo<LeaderboardDisplayRow[]>(() => {
+    if (activeTab === "BALANCE") {
+      return balanceLeaderboard.map((entry) => ({
+        key: `balance-${entry.userId}`,
+        rank: entry.rank,
+        username: entry.username,
+        valueText: valueFormatter.format(entry.balance),
+      }));
+    }
+
+    return roiLeaderboard.map((entry) => ({
+      key: `roi-${entry.userId}`,
+      rank: entry.rank,
+      username: entry.username,
+      valueText: formatRoiPercent(entry.roiPercent),
+    }));
+  }, [activeTab, balanceLeaderboard, roiLeaderboard]);
+
   if (isSessionLoading) {
     return <p className="py-20 text-center text-zinc-500">로딩 중...</p>;
   }
@@ -47,63 +145,71 @@
           isMobile ? "text-xl" : "text-4xl"
         }`}
       >
-        실시간 순위
+        리더보드
       </h1>
 
+      <div className="mb-5 border-b border-zinc-200">
+        <div className="grid grid-cols-2">
+          {LEADERBOARD_TABS.map((tab) => (
+            <button
+              key={tab.id}
+              type="button"
+              onClick={() => setActiveTab(tab.id)}
+              className={`pb-3 text-sm font-semibold transition-colors ${
+                activeTab === tab.id
+                  ? "border-b-2 border-black text-black"
+                  : "text-zinc-400"
+              }`}
+            >
+              {tab.label}
+            </button>
+          ))}
+        </div>
+      </div>
+
       {isLoading ? (
         <p className="text-center">로딩 중...</p>
-      ) : leaderboard.length === 0 ? (
+      ) : errorMessage ? (
+        <p className="text-center text-red-500">{errorMessage}</p>
+      ) : displayRows.length === 0 ? (
         <p className="text-center text-gray-600">
-          아직 순위표를 볼 수 없습니다.
+          아직 {activeTab === "BALANCE" ? "잔고" : "수익률"} 순위표를 볼 수
+          없습니다.
         </p>
       ) : (
         <div>
           <ul className="divide-y divide-transparent">
-            {leaderboard.map((entry, index) => (
+            {displayRows.map((entry) => (
               <li
-                key={entry.userId}
+                key={entry.key}
                 className={`flex items-center ${
                   isMobile ? "px-4" : "px-4"
-                } h-12 ${
-                  index === 0 ? "bg-tokhin-green" : "bg-neutral-50"
-                } rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
+                } h-12 ${getRankBackgroundClass(
+                  entry.rank
+                )} rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
               >
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${
+                  className={`font-base ${getRankTextClass(entry.rank)} ${
                     isMobile
                       ? "text-base w-6 text-center"
                       : "text-lg w-12 text-center"
                   }`}
                 >
-                  {index + 1}
+                  {entry.rank}
                 </span>
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
+                  className={`font-base ${getRankTextClass(
+                    entry.rank
+                  )} flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
                 >
-                  {entry.name}
+                  {entry.username}
                 </span>
                 <span
-                  className={`font-normal ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${isMobile ? "text-base px-1" : "text-2xl"}`}
+                  className={`font-normal ${getRankTextClass(entry.rank)} ${
+                    isMobile ? "text-base px-1" : "text-2xl"
+                  }`}
                 >
-                  {entry.score}
+                  {entry.valueText}
                 </span>
               </li>
             ))}
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/prd.json b/prd.json
index 88f5357a539191312b41088c3cface270425eb93..79cc576c022136f0b53f7c46f944a6e697bb4e50
--- a/prd.json
+++ b/prd.json
@@ -124,7 +124,7 @@
       "id": "US-008",
       "title": "\ub9ac\ub354\ubcf4\ub4dc \u2014 \ucf54\uc778 \uc794\uace0 + \uc218\uc775\ub960 \ubcf5\ud569 \ub7ad\ud0b9",
       "priority": 8,
-      "passes": false,
+      "passes": true,
       "description": "\ub9ac\ub354\ubcf4\ub4dc\ub97c \ucf54\uc778 \uc794\uace0 \uc21c\uc704\uc640 \uc218\uc775\ub960 \uc21c\uc704 \ub450 \uac00\uc9c0 \ud0ed\uc73c\ub85c \uc81c\uacf5\ud55c\ub2e4. \ub514\uc790\uc778\uc740 prd.md\uc758 W-07 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ucc38\uc870.",
       "acceptanceCriteria": [
         "REQ-053 get_leaderboard_balance() RETURNS TABLE(...) RPC \ud568\uc218. \ucf54\uc778 \uc794\uace0 \uae30\uc900 \ub0b4\ub9bc\ucc28\uc21c \uc815\ub82c. \ubc18\ud658: rank, user_id, username, balance.",
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;


thinking
**Summarizing final implementation and next steps**
codex
US-008 구현 완료했습니다.

**변경 내용**
- REQ-053/054: 신규 RPC 2개 추가
  - [20260224_us008_leaderboard_rankings.sql](/Users/jeffrey/WebstormProjects/lookin/lookin/supabase/migrations/20260224_us008_leaderboard_rankings.sql:4)
  - `get_leaderboard_balance()` 잔고 내림차순 + `rank, user_id, username, balance` 반환
  - `get_leaderboard_roi()` 수익률 `(current_balance - total_granted) / total_granted * 100` 계산 + `rank, user_id, username, roi_percent, current_balance, total_granted` 반환
  - `total_granted`는 `transactions.type IN ('WEEKLY_GRANT','ADMIN_GRANT')` 합계로 계산
  - RPC 실행 권한 `anon, authenticated` 부여
- REQ-055: 리더보드 탭 전환 UI 구현
  - [page.tsx](/Users/jeffrey/WebstormProjects/lookin/lookin/app/leaderboard/page.tsx:22)
  - `'잔고 순위' / '수익률 순위'` 탭 추가, 탭별 데이터 렌더링
- REQ-056: 기존 순위 스타일 유지
  - [page.tsx](/Users/jeffrey/WebstormProjects/lookin/lookin/app/leaderboard/page.tsx:36)
  - 1등 `bg-tokhin-green + text-white`, 2~3등 `text-tokhin-green`, 나머지 `text-stone-500`
- API 클라이언트 추가
  - [api.ts](/Users/jeffrey/WebstormProjects/lookin/lookin/lib/api.ts:816)
  - `getLeaderboardBalance()`, `getLeaderboardRoi()` 추가
- 스토리 상태 갱신
  - [prd.json](/Users/jeffrey/WebstormProjects/lookin/lookin/prd.json:124) `US-008.passes: true`

**검증**
- `npm run lint` 통과
- `npm run build` 통과 (타입체크 포함)

다음으로 배포 DB에 새 마이그레이션 적용만 하면 실제 RPC가 동작합니다.
file update:
diff --git a/app/leaderboard/page.tsx b/app/leaderboard/page.tsx
index 2adb2b80e2f8fed380614a6cbd203a475e0a18fa..cf6731cc7b5e350119555e4e2824b14c9a2f3f56
--- a/app/leaderboard/page.tsx
+++ b/app/leaderboard/page.tsx
@@ -1,15 +1,52 @@
 "use client";
 
-import { useState, useEffect } from "react";
-import { getLeaderboard } from "@/lib/api";
+import { useEffect, useMemo, useState } from "react";
+import {
+  getLeaderboardBalance,
+  getLeaderboardRoi,
+  type LeaderboardBalanceItem,
+  type LeaderboardRoiItem,
+} from "@/lib/api";
 import { useIsMobile } from "@/lib/hooks/useResponsive";
 import { useUserSession } from "@/lib/hooks/useUserSession";
 
-type LeaderboardEntry = {
-  userId: string;
-  name: string;
-  student_number: string;
-  score: number;
+type LeaderboardTab = "BALANCE" | "ROI";
+
+type LeaderboardDisplayRow = {
+  key: string;
+  rank: number;
+  username: string;
+  valueText: string;
+};
+
+const LEADERBOARD_TABS: Array<{ id: LeaderboardTab; label: string }> = [
+  { id: "BALANCE", label: "잔고 순위" },
+  { id: "ROI", label: "수익률 순위" },
+];
+
+const valueFormatter = new Intl.NumberFormat("ko-KR", {
+  maximumFractionDigits: 2,
+});
+
+const formatRoiPercent = (value: number) => {
+  const formatted = valueFormatter.format(Math.abs(value));
+  return `${value >= 0 ? "+" : "-"}${formatted}%`;
+};
+
+const getRankTextClass = (rank: number) => {
+  if (rank === 1) {
+    return "text-white";
+  }
+
+  if (rank === 2 || rank === 3) {
+    return "text-tokhin-green";
+  }
+
+  return "text-stone-500";
+};
+
+const getRankBackgroundClass = (rank: number) => {
+  return rank === 1 ? "bg-tokhin-green" : "bg-neutral-50";
 };
 
 export default function LeaderboardPage() {
@@ -17,21 +54,82 @@
   const { session, isLoading: isSessionLoading } = useUserSession({
     requireAuth: true,
   });
-  const [leaderboard, setLeaderboard] = useState<LeaderboardEntry[]>([]);
+
+  const [activeTab, setActiveTab] = useState<LeaderboardTab>("BALANCE");
+  const [balanceLeaderboard, setBalanceLeaderboard] = useState<
+    LeaderboardBalanceItem[]
+  >([]);
+  const [roiLeaderboard, setRoiLeaderboard] = useState<LeaderboardRoiItem[]>([]);
   const [isLoading, setIsLoading] = useState(true);
+  const [errorMessage, setErrorMessage] = useState<string | null>(null);
 
   useEffect(() => {
-    if (!session) return;
+    if (!session?.user_id) {
+      return;
+    }
 
+    let isCancelled = false;
     const fetchLeaderboard = async () => {
       setIsLoading(true);
-      const data = await getLeaderboard("2025-10-05", "2025-12-31");
-      setLeaderboard(data);
-      setIsLoading(false);
+      setErrorMessage(null);
+
+      try {
+        const [balanceData, roiData] = await Promise.all([
+          getLeaderboardBalance(),
+          getLeaderboardRoi(),
+        ]);
+
+        if (isCancelled) {
+          return;
+        }
+
+        setBalanceLeaderboard(balanceData);
+        setRoiLeaderboard(roiData);
+      } catch (error) {
+        if (isCancelled) {
+          return;
+        }
+
+        const message =
+          error instanceof Error
+            ? error.message
+            : "리더보드 조회 중 오류가 발생했습니다";
+
+        setErrorMessage(message);
+        setBalanceLeaderboard([]);
+        setRoiLeaderboard([]);
+      } finally {
+        if (!isCancelled) {
+          setIsLoading(false);
+        }
+      }
     };
+
     fetchLeaderboard();
-  }, [session]);
 
+    return () => {
+      isCancelled = true;
+    };
+  }, [session?.user_id]);
+
+  const displayRows = useMemo<LeaderboardDisplayRow[]>(() => {
+    if (activeTab === "BALANCE") {
+      return balanceLeaderboard.map((entry) => ({
+        key: `balance-${entry.userId}`,
+        rank: entry.rank,
+        username: entry.username,
+        valueText: valueFormatter.format(entry.balance),
+      }));
+    }
+
+    return roiLeaderboard.map((entry) => ({
+      key: `roi-${entry.userId}`,
+      rank: entry.rank,
+      username: entry.username,
+      valueText: formatRoiPercent(entry.roiPercent),
+    }));
+  }, [activeTab, balanceLeaderboard, roiLeaderboard]);
+
   if (isSessionLoading) {
     return <p className="py-20 text-center text-zinc-500">로딩 중...</p>;
   }
@@ -47,63 +145,71 @@
           isMobile ? "text-xl" : "text-4xl"
         }`}
       >
-        실시간 순위
+        리더보드
       </h1>
 
+      <div className="mb-5 border-b border-zinc-200">
+        <div className="grid grid-cols-2">
+          {LEADERBOARD_TABS.map((tab) => (
+            <button
+              key={tab.id}
+              type="button"
+              onClick={() => setActiveTab(tab.id)}
+              className={`pb-3 text-sm font-semibold transition-colors ${
+                activeTab === tab.id
+                  ? "border-b-2 border-black text-black"
+                  : "text-zinc-400"
+              }`}
+            >
+              {tab.label}
+            </button>
+          ))}
+        </div>
+      </div>
+
       {isLoading ? (
         <p className="text-center">로딩 중...</p>
-      ) : leaderboard.length === 0 ? (
+      ) : errorMessage ? (
+        <p className="text-center text-red-500">{errorMessage}</p>
+      ) : displayRows.length === 0 ? (
         <p className="text-center text-gray-600">
-          아직 순위표를 볼 수 없습니다.
+          아직 {activeTab === "BALANCE" ? "잔고" : "수익률"} 순위표를 볼 수
+          없습니다.
         </p>
       ) : (
         <div>
           <ul className="divide-y divide-transparent">
-            {leaderboard.map((entry, index) => (
+            {displayRows.map((entry) => (
               <li
-                key={entry.userId}
+                key={entry.key}
                 className={`flex items-center ${
                   isMobile ? "px-4" : "px-4"
-                } h-12 ${
-                  index === 0 ? "bg-tokhin-green" : "bg-neutral-50"
-                } rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
+                } h-12 ${getRankBackgroundClass(
+                  entry.rank
+                )} rounded-lg shadow-[0px_0px_8px_0px_rgba(0,0,0,0.12)] my-2`}
               >
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${
+                  className={`font-base ${getRankTextClass(entry.rank)} ${
                     isMobile
                       ? "text-base w-6 text-center"
                       : "text-lg w-12 text-center"
                   }`}
                 >
-                  {index + 1}
+                  {entry.rank}
                 </span>
                 <span
-                  className={`font-base ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
+                  className={`font-base ${getRankTextClass(
+                    entry.rank
+                  )} flex-grow ${isMobile ? "text-base px-2" : "text-xl"}`}
                 >
-                  {entry.name}
+                  {entry.username}
                 </span>
                 <span
-                  className={`font-normal ${
-                    index === 0
-                      ? "text-white"
-                      : index === 1 || index === 2
-                      ? "text-tokhin-green"
-                      : "text-stone-500"
-                  } ${isMobile ? "text-base px-1" : "text-2xl"}`}
+                  className={`font-normal ${getRankTextClass(entry.rank)} ${
+                    isMobile ? "text-base px-1" : "text-2xl"
+                  }`}
                 >
-                  {entry.score}
+                  {entry.valueText}
                 </span>
               </li>
             ))}
diff --git a/lib/api.ts b/lib/api.ts
index deca001073cf308939e36a797cd17f787aa57fb4..153071489962c6086bdcad2679ea9a6a7c6be68e
--- a/lib/api.ts
+++ b/lib/api.ts
@@ -170,6 +170,22 @@
   away_team: TeamRelation;
 }
 
+interface LeaderboardBalanceRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  balance: number | string;
+}
+
+interface LeaderboardRoiRpcRow {
+  rank: number | string;
+  user_id: string;
+  username: string;
+  roi_percent: number | string;
+  current_balance: number | string;
+  total_granted: number | string;
+}
+
 export interface MarketListItem {
   id: number;
   gameId: number;
@@ -269,6 +285,22 @@
   awayTeamShortName: string | null;
 }
 
+export interface LeaderboardBalanceItem {
+  rank: number;
+  userId: string;
+  username: string;
+  balance: number;
+}
+
+export interface LeaderboardRoiItem {
+  rank: number;
+  userId: string;
+  username: string;
+  roiPercent: number;
+  currentBalance: number;
+  totalGranted: number;
+}
+
 const normalizeTeamRelation = (team: TeamRelation): TeamWithShortName | null => {
   if (Array.isArray(team)) {
     return team[0] ?? null;
@@ -780,31 +812,45 @@
   return { games: combinedData, totalPoints };
 };
 
-// 5. Fetch Leaderboard
-export const getLeaderboard = async (startDate?: string, endDate?: string) => {
-  const { data, error } = await supabase.rpc("get_leaderboard", {
-    start_date: startDate,
-    end_date: endDate,
-  });
+// 5. Fetch Leaderboard (US-008)
+export const getLeaderboardBalance = async (): Promise<
+  LeaderboardBalanceItem[]
+> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_balance");
+
+  if (error) {
+    console.error("Error calling get_leaderboard_balance RPC:", error);
+    throw new Error("리더보드 잔고 순위 조회 중 오류가 발생했습니다");
+  }
+
+  const rows = (data ?? []) as LeaderboardBalanceRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    balance: toNumberOrNull(entry.balance) ?? 0,
+  }));
+};
+
+export const getLeaderboardRoi = async (): Promise<LeaderboardRoiItem[]> => {
+  const { data, error } = await supabase.rpc("get_leaderboard_roi");
 
   if (error) {
-    console.error("Error fetching leaderboard:", error);
-    return [];
+    console.error("Error calling get_leaderboard_roi RPC:", error);
+    throw new Error("리더보드 수익률 순위 조회 중 오류가 발생했습니다");
   }
 
-  return data.map(
-    (entry: {
-      user_id: string;
-      username: string;
-      student_number: string;
-      total_score: number;
-    }) => ({
-      userId: entry.user_id,
-      name: entry.username,
-      student_number: entry.student_number,
-      score: entry.total_score,
-    })
-  );
+  const rows = (data ?? []) as LeaderboardRoiRpcRow[];
+
+  return rows.map((entry) => ({
+    rank: Math.trunc(toNumberOrNull(entry.rank) ?? 0),
+    userId: entry.user_id,
+    username: entry.username,
+    roiPercent: toNumberOrNull(entry.roi_percent) ?? 0,
+    currentBalance: toNumberOrNull(entry.current_balance) ?? 0,
+    totalGranted: toNumberOrNull(entry.total_granted) ?? 0,
+  }));
 };
 
 // 6. Fetch Prediction Ratios for a specific date
diff --git a/prd.json b/prd.json
index 88f5357a539191312b41088c3cface270425eb93..79cc576c022136f0b53f7c46f944a6e697bb4e50
--- a/prd.json
+++ b/prd.json
@@ -124,7 +124,7 @@
       "id": "US-008",
       "title": "\ub9ac\ub354\ubcf4\ub4dc \u2014 \ucf54\uc778 \uc794\uace0 + \uc218\uc775\ub960 \ubcf5\ud569 \ub7ad\ud0b9",
       "priority": 8,
-      "passes": false,
+      "passes": true,
       "description": "\ub9ac\ub354\ubcf4\ub4dc\ub97c \ucf54\uc778 \uc794\uace0 \uc21c\uc704\uc640 \uc218\uc775\ub960 \uc21c\uc704 \ub450 \uac00\uc9c0 \ud0ed\uc73c\ub85c \uc81c\uacf5\ud55c\ub2e4. \ub514\uc790\uc778\uc740 prd.md\uc758 W-07 \uc640\uc774\uc5b4\ud504\ub808\uc784 \ucc38\uc870.",
       "acceptanceCriteria": [
         "REQ-053 get_leaderboard_balance() RETURNS TABLE(...) RPC \ud568\uc218. \ucf54\uc778 \uc794\uace0 \uae30\uc900 \ub0b4\ub9bc\ucc28\uc21c \uc815\ub82c. \ubc18\ud658: rank, user_id, username, balance.",
diff --git a/supabase/migrations/20260224_us008_leaderboard_rankings.sql b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
new file mode 100644
index 0000000000000000000000000000000000000000..1d8425d2ecf76f3a15b49dba2b8c9fe074810774
--- /dev/null
+++ b/supabase/migrations/20260224_us008_leaderboard_rankings.sql
@@ -0,0 +1,133 @@
+-- US-008: 리더보드 — 코인 잔고 + 수익률 복합 랭킹
+
+-- REQ-053: 코인 잔고 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_balance()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  balance NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH leaderboard AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS balance
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          l.balance DESC,
+          l.username ASC,
+          l.user_id ASC
+      )::BIGINT AS position_rank,
+      l.user_id,
+      l.username,
+      l.balance
+    FROM leaderboard l
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.balance
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+-- REQ-054: 수익률 순위 조회
+CREATE OR REPLACE FUNCTION public.get_leaderboard_roi()
+RETURNS TABLE(
+  rank BIGINT,
+  user_id UUID,
+  username TEXT,
+  roi_percent NUMERIC,
+  current_balance NUMERIC,
+  total_granted NUMERIC
+)
+LANGUAGE plpgsql
+SECURITY DEFINER
+SET search_path = public
+AS $$
+BEGIN
+  RETURN QUERY
+  WITH granted AS (
+    SELECT
+      t.user_id,
+      COALESCE(SUM(t.amount), 0) AS total_granted
+    FROM public.transactions t
+    WHERE t.type IN ('WEEKLY_GRANT', 'ADMIN_GRANT')
+    GROUP BY t.user_id
+  ),
+  roi_base AS (
+    SELECT
+      u.id AS user_id,
+      u.username,
+      COALESCE(w.balance, 0) AS current_balance,
+      COALESCE(g.total_granted, 0) AS total_granted
+    FROM public.users u
+    LEFT JOIN public.wallets w
+      ON w.user_id = u.id
+    LEFT JOIN granted g
+      ON g.user_id = u.id
+  ),
+  scored AS (
+    SELECT
+      rb.user_id,
+      rb.username,
+      rb.current_balance,
+      rb.total_granted,
+      CASE
+        WHEN rb.total_granted > 0
+          THEN ((rb.current_balance - rb.total_granted) / rb.total_granted) * 100
+        ELSE 0
+      END AS roi_percent
+    FROM roi_base rb
+  ),
+  ranked AS (
+    SELECT
+      RANK() OVER (
+        ORDER BY
+          s.roi_percent DESC,
+          s.current_balance DESC,
+          s.username ASC,
+          s.user_id ASC
+      )::BIGINT AS position_rank,
+      s.user_id,
+      s.username,
+      s.roi_percent,
+      s.current_balance,
+      s.total_granted
+    FROM scored s
+  )
+  SELECT
+    r.position_rank AS rank,
+    r.user_id,
+    r.username,
+    r.roi_percent,
+    r.current_balance,
+    r.total_granted
+  FROM ranked r
+  ORDER BY
+    r.position_rank ASC,
+    r.username ASC,
+    r.user_id ASC;
+END;
+$$;
+
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_balance() TO anon, authenticated;
+GRANT EXECUTE ON FUNCTION public.get_leaderboard_roi() TO anon, authenticated;

tokens used
77,470
US-008 구현 완료했습니다.

**변경 내용**
- REQ-053/054: 신규 RPC 2개 추가
  - [20260224_us008_leaderboard_rankings.sql](/Users/jeffrey/WebstormProjects/lookin/lookin/supabase/migrations/20260224_us008_leaderboard_rankings.sql:4)
  - `get_leaderboard_balance()` 잔고 내림차순 + `rank, user_id, username, balance` 반환
  - `get_leaderboard_roi()` 수익률 `(current_balance - total_granted) / total_granted * 100` 계산 + `rank, user_id, username, roi_percent, current_balance, total_granted` 반환
  - `total_granted`는 `transactions.type IN ('WEEKLY_GRANT','ADMIN_GRANT')` 합계로 계산
  - RPC 실행 권한 `anon, authenticated` 부여
- REQ-055: 리더보드 탭 전환 UI 구현
  - [page.tsx](/Users/jeffrey/WebstormProjects/lookin/lookin/app/leaderboard/page.tsx:22)
  - `'잔고 순위' / '수익률 순위'` 탭 추가, 탭별 데이터 렌더링
- REQ-056: 기존 순위 스타일 유지
  - [page.tsx](/Users/jeffrey/WebstormProjects/lookin/lookin/app/leaderboard/page.tsx:36)
  - 1등 `bg-tokhin-green + text-white`, 2~3등 `text-tokhin-green`, 나머지 `text-stone-500`
- API 클라이언트 추가
  - [api.ts](/Users/jeffrey/WebstormProjects/lookin/lookin/lib/api.ts:816)
  - `getLeaderboardBalance()`, `getLeaderboardRoi()` 추가
- 스토리 상태 갱신
  - [prd.json](/Users/jeffrey/WebstormProjects/lookin/lookin/prd.json:124) `US-008.passes: true`

**검증**
- `npm run lint` 통과
- `npm run build` 통과 (타입체크 포함)

다음으로 배포 DB에 새 마이그레이션 적용만 하면 실제 RPC가 동작합니다.
