begin;select plan(4);select has_table('public','season_incidents','incidents persist');select ok((preview_season_incident(1))?'counts','impact counts shown');select ok(not(preview_season_incident(1)->>'automatic_possible')::boolean,'active season activity blocks unsafe recovery');select ok((select count(*)from seasons where status='ACTIVE')=1,'exactly one active season');select*from finish();rollback;
